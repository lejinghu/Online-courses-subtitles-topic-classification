目前,最常用的一维数组是字符串。在C语言中，串定义为一维字符型数组，以null(空）字符结尾（空字 符就是0值)。因此，字符串包含的字符是由后接空字符的串构成的。以空字符结尾的字符串是C语言定义 的惟一字符串类型。
编译后，程序照样能正常工作，因为C编译程序产生的代码就是令fbnclO接受指针，并不生成32元素的数 组。
4.4串
注意:C++也定义了字符串类，叫做string,它为字符串处理提供了面向对象的方法，但C语言不支持它。
定义字符串数组时,数组长度必须是其中最多字符数加1。例如，声明拥有长度为10的字符串的数组8tr 时，应该书写为
目前,最常用的一维数组是字符串。在C语言中，串定义为一维字符型数组，以null(空）字符结尾（空字 符就是0值)。因此，字符串包含的字符是由后接空字符的串构成的。以空字符结尾的字符串是C语言定义 的惟一字符串类型。
编译后，程序照样能正常工作，因为C编译程序产生的代码就是令fbnclO接受指针，并不生成32元素的数 组。
4.4串
注意:C++也定义了字符串类，叫做string,它为字符串处理提供了面向对象的方法，但C语言不支持它。
定义字符串数组时,数组长度必须是其中最多字符数加1。例如，声明拥有长度为10的字符串的数组8tr 时，应该书写为
