4) 当字符型数据赋给一个整型变量时，如将'A'赋给int型变量x,即x='A'，不同的 系统实现的情况不同，一般当该字符的ASCII值小于127时，系统将整型变量的高字节 置0，低字节存放该字符的ASCII值，所以此时的x=字符A的ASCII值65。
变量时，如将1.2赋给int型变量啊，即a=0.9，系统会自动舍弃小数部分，此时a=3。
3)	将整数赋给一个浮点型变量时，如将34赋给float型变量c,即c=12,系统将保 持数值不变并且以浮点小数形式存储到变量中，此时c=12.⑻⑻00。
例0.4:下面的程序给出了一个赋值时类型自动转换的示例。
#include <stdio.h>
4) 当字符型数据赋给一个整型变量时，如将'A'赋给int型变量x,即x='A'，不同的 系统实现的情况不同，一般当该字符的ASCII值小于127时，系统将整型变量的高字节 置0，低字节存放该字符的ASCII值，所以此时的x=字符A的ASCII值65。
变量时，如将1.2赋给int型变量啊，即a=0.9，系统会自动舍弃小数部分，此时a=3。
3)	将整数赋给一个浮点型变量时，如将34赋给float型变量c,即c=12,系统将保 持数值不变并且以浮点小数形式存储到变量中，此时c=12.⑻⑻00。
例0.4:下面的程序给出了一个赋值时类型自动转换的示例。
#include <stdio.h>
