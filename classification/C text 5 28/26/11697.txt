定义字符变量C后，将它初始化为字符'Q。在程序的下一行代码中，变 量char_pointer被定义为“指向char的指针”类型，也就是说，无论在 这个变量中存储什么值，都应当将其看作是一个字符的间接引用（指针)。注意， 可以采用通常方式为这一变量赋一个初始值。在程序中，为char_pointer 赋的值是指向变量c的指针，它是通过向变量c应用地址运算符得到的。（注 意，如果c是在这个语句之后定义的，则这个初始化会造成编译器错误，这是 因为，在表达式中引用一个变量的取值之前，必须先声明该变量。）
程序10.2输出
Q Q
变量char_pointer的声明及其初始值的指定可以用两个独立语句做如 下等价表示：
char ★char—pointer; char_pointer = &c;
