?字符型 char型用来表示一个单独的字符。因为char型的值被定义为只需要一个字节 的存储空间，因此宇符型是表达数据值大小的基本单元。
?整型基本的整数类型是int，它对应于一个有符号整数.表示整数的位数随机器的不 同而不同，从而影响到int型的最大值。所有兼容ANSI的编译器必须允许int型至少有 16位，也就是说它们的数值范围至少必须为-32 768至32 767。为了使编译器能够为一 个整数分配更多或更少的空间，整型int可以用long和short加以修饰，此外，整型 int, short和long可以单独被signed或unsighed修饰，表示是否允许负值。当使 用修饰符时，可以省略类型名int。
?浮点数C语言定义了三种浮点类型：float、double和long doubly这几种类型 的精度和所需的存储空间取决干编译器和基本的硬件。对大部分应用来说，用double 型就够了。
?牧举类型除了内置的原子类型外，C语言还允许你定义枚举类型，它们的值是有限个 已命名的常量的集合。在本书中，用来引入一个新的枚举类型的标准句法是：
typedef e n um {list of e iements} type n arne ；	[6591
