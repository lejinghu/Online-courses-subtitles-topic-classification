r定义一个指向FILE类型结构体的指针变置V /*定义变量及数组为字符型V /*以只读方式打开指定文件V /"fgetc函数带回一个字符賦给ch*/ r当读入的字符值等于EOF时结束循环V
#include <stdio.h> main()
{
广将读入的字符输出在屏幕上*/
/?fgetc函数继续带回一个字符賦给chV
r定义一个指向FILE类型结构体的指针变置V /*定义变量及数组为字符型V /*以只读方式打开指定文件V /"fgetc函数带回一个字符賦给ch*/ r当读入的字符值等于EOF时结束循环V
#include <stdio.h> main()
{
广将读入的字符输出在屏幕上*/
/?fgetc函数继续带回一个字符賦给chV
r定义一个指向FILE类型结构体的指针变置V /*定义变量及数组为字符型V /*以只读方式打开指定文件V /"fgetc函数带回一个字符賦给ch*/ r当读入的字符值等于EOF时结束循环V
#include <stdio.h> main()
{
广将读入的字符输出在屏幕上*/
/?fgetc函数继续带回一个字符賦给chV
