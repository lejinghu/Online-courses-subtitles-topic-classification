表示声明变量cptr为指向字符型的类型，虽然这两种类型在计算机内部都是以地址形式表示的， 但指向整型的指针和指向字符型的指针在C语言中还是有区別的。要使用该地址中的数据，编译 程序必须知道如何解释地址里的数据，因此就要求显式地说明指针所指向的数据的类型。指针 指向的值的类型称为指针的基本类型（base type)。所以，指向整型的指针的基本类型为int。
将变量P声明为槪念上指向整型的类型。类似的，声明
char cptr；
语法:指针声明
base-type  pointer-variable ??
