?第11行和第14行：字符变量’C被转换成整数67 ,然后把该整数加1.结果的4 字节的整数68被截为1字节并存储在ch里。当使用％c说明符进行打印时，68被 览释为‘D的ASCII码。 ?第12行和第14行：为了和2相乘，ch的值被转换为一个4字节的整数（68 )。乘 积整数（136)为了和lf相加而被转换为浮点类型。结果（203.00f)被转换成int类 型并存储在i中。
下面是在8位char, 32位int的系统上运行程序的过程分析：
?第9行和第10行：字符‘C被作为1字节的ASCII值存储在ch里。整数变量i接 受由‘C转换成的整数，即67,它以4个字节存储。最后，fl接受由67转换成的浮 点数，即67.00。
?第13行和第14行：为了和2.0相乘，ch的值（’D',即68)被转换为浮点类型。 为了做加去，i的值（203 )被转换为浮点类型。结果（339.00 )被存储在fl中。
?第15行和第16行：在这里，示例程序尝试了一个降级的实例，把ch设置为一个很 有大的数。在截去髙位后，ch最终变成连字符这一字符的ASCII码。
