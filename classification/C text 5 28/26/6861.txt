字符型数据包括字符常量和字符变量。字符常量除了普通的字符以外还包含转义字 符，字符变量用来存放字符，一个字符变量只能存放一个字符。
口程序第10行使用％lf格式说明符来输出变量z的值，其输出结果中保留了 6位 小数。
4.2.3字符型数据
1.字符常量
字符常量是使用单引号括起来的一个字符。例如，’a'、V、'?’和都是合法的字符常 量。需要注意的是'a'与'A'代表的是不同的字符。
字符型数据包括字符常量和字符变量。字符常量除了普通的字符以外还包含转义字 符，字符变量用来存放字符，一个字符变量只能存放一个字符。
口程序第10行使用％lf格式说明符来输出变量z的值，其输出结果中保留了 6位 小数。
4.2.3字符型数据
1.字符常量
字符常量是使用单引号括起来的一个字符。例如，’a'、V、'?’和都是合法的字符常 量。需要注意的是'a'与'A'代表的是不同的字符。
