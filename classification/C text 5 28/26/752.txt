char b[40j;	说明5数组为字符型，是一维数组，其中有40个数组元素。
如：
floatmath|lOj; 说明〇^出数组为浮点类型，是一维数组，其中有10个数组元素。 int a|8];	说明3数组为整型类型，是一维数组，其中有8个数组元素。
(2>数组名的命名规则与普通变量相同。
(3)	常量表达式的值是数组中元素的个数，由于0：语言中数组的下标从0开始，因此其 最大下标为：常量表达式的值-1。
