字符型数据是由单引号括起来的单个字符，不能是多个。转义字符是特殊的字符。不同的数据 类型，在计算机中占用的存储空间是不一样的。
关于字符与对应的ASCII码之间的关系见附录2。

3.1.6什么符号可以作为变量名——用户定义的标识符
那什么样的符号可以作为变量名呢？ c语言规定，变量名只能由字母、数字和下划线构成。 其中，第一个字符必须是字母或下划线。变量名可以由1个字符构成，也可以由多个字符构成， 但最长不能超过32个字符。变量名也要用标识符定义，变童名是一种用户自己定义的标识符。 例如，_abc、add、average、vc_study、score3、sum_l都是正确的变量名。下面都是不正确的变
