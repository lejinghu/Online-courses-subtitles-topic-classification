(2)	也是一个字符变量名或字符常量。
说明：
(1)	[口此《:函数适合于一个字符的输出》
(3)	〖？是一个己经指向某个文件的文件指针。
注意：8<^化31()函数和口讲(^耐）函数也是一对适合于字符的输入输出函数，但其输入来 自于标准输入设备——键盘，其输出流入标准输出设备——显示器；而fputc()函数和fgetc() 函数的数据流向是磁盘文件，即输入的数据取自于磁盘文件，输出的数据写入磁盘文件。 【例9-丨】从键盘输入10个字符，写入文件12办1中。 编写代码9-1如下，其执行结果如图9-3(3)所示。
