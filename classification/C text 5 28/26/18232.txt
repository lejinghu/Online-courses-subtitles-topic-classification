该函数的作用是把一个字符写到fp所指向的文件中去。其中ch是要输出的字符，它可以 是一个字符常量，也可以是一个字符变量；fj5是文件指针变量。当函数输出成功则返回值就是 输出的字符；如果输出失败，则返回EOF (即-1), EOF是在stdio.h文件中定义的符号常量，
fputc(ch,fp);

值为-1。
实例16.1编程实现向E:\Cyuyan01.txt中写入“好好学习fj)utc〇函数，不难熟练掌握！ ”，
