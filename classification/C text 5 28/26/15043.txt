在cnvt中，整型i和字符型ch两个变童共享同一片内存。i占两字节(假定为两字节整数），ch占一字节， 如图7-2所示。无论何时，程序中都能通过i按整型访问cnvt中的数据，也可通过ch按字节访问cnvt中的数 据。
联合定义并不声明变量。变童的声明既可以通过把变量名放到定义末尾进行，也可以用联合类型专门进行。 用刚刚定义的u_ type可以声明u_ type类的联合变元cnvt,如下所示：
union u_ type cnvt;
字节0	字节1

