为了让程序能输入、处理和输出字符串数据，接下来需要学习：如何使用 字符变量的特殊类型储存字符串数据。
在程序中添加字符串
虽然C语言没有字符串变量，但是也可以储存字符串数据。本章将介绍C 语言储存字符串数据的方式。前面己经介绍过过，必须用双引号将字符串括起 来，即使将单个字符放在双引号中，也是字符串。另外，也熟悉了使用printf() 打印字符串。
6.1字符串结束符
c语言对字符串的处理方式很奇怪：在每个字符串末尾加一个零。字符串末尾的零有 如下几种名称：
为了让程序能输入、处理和输出字符串数据，接下来需要学习：如何使用 字符变量的特殊类型储存字符串数据。
在程序中添加字符串
虽然C语言没有字符串变量，但是也可以储存字符串数据。本章将介绍C 语言储存字符串数据的方式。前面己经介绍过过，必须用双引号将字符串括起 来，即使将单个字符放在双引号中，也是字符串。另外，也熟悉了使用printf() 打印字符串。
6.1字符串结束符
c语言对字符串的处理方式很奇怪：在每个字符串末尾加一个零。字符串末尾的零有 如下几种名称：
