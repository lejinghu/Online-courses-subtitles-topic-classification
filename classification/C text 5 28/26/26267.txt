字符型和整型（int型、long型等）能够表示的数值范围之所以因编译器而异，是因为在 内存上占据的位数因编译器而异。
如果CHAR_BIT为8，则char型的内部如图7-7所示。
当然，能够用字符型表示的数值的范围，是依存于CHAR_BIT的。
sizeof运算符
C语言中将表示字符的char型的长度定义为1。通过使用sizeof运算符（sizeof operator)， 可以判断出包括char型在内的所有数据类型的长度，如表74所示。
