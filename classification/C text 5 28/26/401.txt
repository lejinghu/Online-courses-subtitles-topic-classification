字符变量=fgetc(文件指针）；/从文件读取一个字符给变量/
18.2.3文本文件的读写操作
fputc和fgetc是文本文件两个最常用的读写库函数，其使用格式如下： fputc(字符，文件指针）；	/将一个字符写入文件/
如果文件已读到结尾，fgetc读到的值是EOF,即-1。也可以使用库函数feof来判断 是否己读到文件结尾，其使用格式为：
feof(文件指针）/其值为1表明已到文件结尾，否则为0/
