实例10.11中定义了两个指向字符型数据的指针变量。首先让pi和p2分别指向字符串a和字符串 b的第一个字符的地址。将pi所指向的内容賦给p2所指向的元素，然后pl和P2分别加1,指向下一 个元素，直到*pl的值为W为止。

图10.21连接两个字符串
这里有一点需要注意，就是pl和p2的值是同步变化的，如图10.22所示，若pl处在pll的位置， p2就处在p21的位置：若pl处在pl2的位置，p2就处在p22的位置。
pi a	p2 b
实例10.11中定义了两个指向字符型数据的指针变量。首先让pi和p2分别指向字符串a和字符串 b的第一个字符的地址。将pi所指向的内容賦给p2所指向的元素，然后pl和P2分别加1,指向下一 个元素，直到*pl的值为W为止。

图10.21连接两个字符串
这里有一点需要注意，就是pl和p2的值是同步变化的，如图10.22所示，若pl处在pll的位置， p2就处在p21的位置：若pl处在pl2的位置，p2就处在p22的位置。
pi a	p2 b
