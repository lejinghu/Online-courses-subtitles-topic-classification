字符变量用来存放字符，一个字符变量只能存放一个字符，不要误以为在字符变量 中可以存放字符串。
至此，屏幕上已经按照程序要求输出了全部的字符，包含一般的可显示字符及转义 字符。
2.字符变量
字符变量的类型说明符为char,其定义形式和书写规则都与整型变量相同。例如：
char a,b;
字符变量用来存放字符，一个字符变量只能存放一个字符，不要误以为在字符变量 中可以存放字符串。
至此，屏幕上已经按照程序要求输出了全部的字符，包含一般的可显示字符及转义 字符。
2.字符变量
字符变量的类型说明符为char,其定义形式和书写规则都与整型变量相同。例如：
char a,b;
