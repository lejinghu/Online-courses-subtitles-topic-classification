对结构体变量赋初值时，C编译程序按每个成员在结构体中的顺序一一对应赋初值，不允许 跳过前面的成员给后面的成员赋初值，但可以只给前面的若干个成员赋初值，对于后面未赋初值 的成员，系统将自动为数值型和字符型数据赋初值零。
Li Ming	M	1962	5	10	88.0	76.0	85.5	90.0
图14.2赋初值后变量.std的内容
2.给结构体数组赋初值
给结构体数组赋初值的规则与第9章中介绍的相同。只是由于数组中的每个元素都是一个 结构体，因此通常将其成员的值依次放在一对花括号中，以便区分各个元素。例如：
对结构体变量赋初值时，C编译程序按每个成员在结构体中的顺序一一对应赋初值，不允许 跳过前面的成员给后面的成员赋初值，但可以只给前面的若干个成员赋初值，对于后面未赋初值 的成员，系统将自动为数值型和字符型数据赋初值零。
Li Ming	M	1962	5	10	88.0	76.0	85.5	90.0
图14.2赋初值后变量.std的内容
2.给结构体数组赋初值
给结构体数组赋初值的规则与第9章中介绍的相同。只是由于数组中的每个元素都是一个 结构体，因此通常将其成员的值依次放在一对花括号中，以便区分各个元素。例如：
