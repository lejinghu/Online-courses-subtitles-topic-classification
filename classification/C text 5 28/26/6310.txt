■	在使用字符I/O函数时，不要使用字符变量，要用int变量。
■	如果不想获取Ente「键的按下，让getchar ()单独成行，以处理Enter键的按 下残留。也可以创建一个循环调用getchar()直至返回值是\n,如本章代码示 例所示。
■	使用getch <)在用户键入时获取无缓冲的单个字符。
■	如果希望在用户键入内容的同时，将其显示在屏幕上，要使用putch ()来打印 输入的字符。
本章内容
