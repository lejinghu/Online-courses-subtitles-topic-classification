上面三个声明语句分别将pci和pc2声明成为可以指向字符变量的字符指针，将pi声明成为可 以指向整型变量的整型指针，将pf声明成为可以指向浮点型变量的浮点型指针。
【例8.1】声明指针并解释其含义。
char *pclf *pc2; int *pi; float *pf;
指针变量也可以在声明的时候对其进行初始化，但必须用所指变量的地址进行初始化。初始化 后的指针将指向以初值为地址的变量。
【例8.2】声明指针并初始化，并且解释其含义。
上面三个声明语句分别将pci和pc2声明成为可以指向字符变量的字符指针，将pi声明成为可 以指向整型变量的整型指针，将pf声明成为可以指向浮点型变量的浮点型指针。
【例8.1】声明指针并解释其含义。
char *pclf *pc2; int *pi; float *pf;
指针变量也可以在声明的时候对其进行初始化，但必须用所指变量的地址进行初始化。初始化 后的指针将指向以初值为地址的变量。
【例8.2】声明指针并初始化，并且解释其含义。
