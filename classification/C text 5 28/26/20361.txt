其中，void是类型说明符，printX是函数名，第3行是函数的声明部分，第4行和第5行 是函数的语句部分。该函数主要是将字符’A'转换为对应的小写字母'a',并且输出小写字母。该 函数首先定义了一个字符变量c并且将’A赋值给c，然后将大写字母转换为小写字母，最后输 出小写字母。
05	printf(”％c\n"，c);	/*语句部分V
06	}	
如果以上功能是在main函数中完成，代码如下：
01	void main()		/*类型说明符和函数名*/
其中，void是类型说明符，printX是函数名，第3行是函数的声明部分，第4行和第5行 是函数的语句部分。该函数主要是将字符’A'转换为对应的小写字母'a',并且输出小写字母。该 函数首先定义了一个字符变量c并且将’A赋值给c，然后将大写字母转换为小写字母，最后输 出小写字母。
05	printf(”％c\n"，c);	/*语句部分V
06	}	
如果以上功能是在main函数中完成，代码如下：
01	void main()		/*类型说明符和函数名*/
