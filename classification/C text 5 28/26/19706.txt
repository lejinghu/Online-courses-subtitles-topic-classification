在定义了一个字符型变量后，就可以将一个字符存放到字符变量中。字符变量的赋值方法 与整型变量的赋值方法一样，只是字符数据需要有一对单引号括起来。例如，代码如下：
其中，cl、c2、c3是字符型变量。
2.	变敁赋值初步——如h将卞符数裾存放到字符增变a屮
01	c1='A';	/将字符TV赋值给变量c1/
02	c2=’B’；	/将字符丑赋值给变量c2/
在定义了一个字符型变量后，就可以将一个字符存放到字符变量中。字符变量的赋值方法 与整型变量的赋值方法一样，只是字符数据需要有一对单引号括起来。例如，代码如下：
其中，cl、c2、c3是字符型变量。
2.	变敁赋值初步——如h将卞符数裾存放到字符增变a屮
01	c1='A';	/将字符TV赋值给变量c1/
02	c2=’B’；	/将字符丑赋值给变量c2/
