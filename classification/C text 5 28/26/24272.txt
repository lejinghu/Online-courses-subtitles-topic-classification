^注意：在字符型常量和字符串型常量中，引号中的字符不可以是单引号、双引号和反 斜杠‘V。如果要表示这些特殊的字符，需要使用转义字符表示。例如，要表示 双引号，表示为‘V”。
图3.11字符串“Hello”的存储形式
在字符串常量与字符常量中，所有元素都是字符。为什么有了字符常量，还要有字符 串常量呢？因为在编程过程中，使用字符串常量有时会更省事、更方便。
3.5.5常量与常数
常量是由const定义和由define定义(将在第10章介绍)的不可改变的量。而常数则是C 语言中任何类型的数据。可以将常数定义为常量，主要由const定义。由define定义的常量 称为符号常量。例如：
^注意：在字符型常量和字符串型常量中，引号中的字符不可以是单引号、双引号和反 斜杠‘V。如果要表示这些特殊的字符，需要使用转义字符表示。例如，要表示 双引号，表示为‘V”。
图3.11字符串“Hello”的存储形式
在字符串常量与字符常量中，所有元素都是字符。为什么有了字符常量，还要有字符 串常量呢？因为在编程过程中，使用字符串常量有时会更省事、更方便。
3.5.5常量与常数
常量是由const定义和由define定义(将在第10章介绍)的不可改变的量。而常数则是C 语言中任何类型的数据。可以将常数定义为常量，主要由const定义。由define定义的常量 称为符号常量。例如：
