如果17#是字符类型（^—)，那么3^6以操作符的结果为1如果3是一个己经指定
那么下面的表达式：
si2eof(type) 表达式的值等于容纳指定数据类型的值所露要的内存字节数； 5i2e〇f(d)	表达式的值等于保存表达式曰的结果所需的内存字节数>
了维数的数组名（既可以显式指定，也可以通过初始化列表隐式指定)，而且8不是函数的
形式参数或者没有指定长度的外部数组变纛，那么4^〇￡操作符的结果为容纳该数组所有
