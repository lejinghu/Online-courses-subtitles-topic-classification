因为字符型在内存中仅占一个字节，所以执行“c = a;”后，char型变量c只取hit型变量a 值的低8位。程序中primf〇是格式化输出标准函数，它可将a、c、b的值按指定的格式显示在 屏幕上。getch()也是标准函数，等待用户输入任意一个字符。
程序的运行结果为：
350, 94,94
一般来说，一个完整的计算机程序经常要求具备输入和输出功能。C语言本身没有配备完 成输入/输出功能的语句，它的输入/输出功能是通过在程序中调用系统提供的标准函数来实现 的。下面介绍几种常用的I/O (输入/输出）标准函数。
2.4	敖旖的餘出
