到目前为止，我们使用的数据都是基本类型的数据，如整型、实型和字符型。在第 4章我们介绍过，C语言中提供了对基本类型的支持，此外，C语言中还提供了对构造 类型数据的支持，这些类型包括：数据、结构体和共用体。实际上，构造类型数据是由 基本类型数据按照一定的规则组织起来的，因此也常被称为“导出类型”。本章介绍数 组，主要内容如下：
10.	编写程序，将五级记分成绩转换成百分制成绩，转换规则如下：’A'—95, ’B'—85, 'C’一75，'D，一65，'E，一55。
第7章数 组
口数组的概述；
口一维数组：
到目前为止，我们使用的数据都是基本类型的数据，如整型、实型和字符型。在第 4章我们介绍过，C语言中提供了对基本类型的支持，此外，C语言中还提供了对构造 类型数据的支持，这些类型包括：数据、结构体和共用体。实际上，构造类型数据是由 基本类型数据按照一定的规则组织起来的，因此也常被称为“导出类型”。本章介绍数 组，主要内容如下：
10.	编写程序，将五级记分成绩转换成百分制成绩，转换规则如下：’A'—95, ’B'—85, 'C’一75，'D，一65，'E，一55。
第7章数 组
口数组的概述；
口一维数组：
