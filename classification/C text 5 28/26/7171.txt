□程序第15行定义了指针数组keyword和指向字符型数组的指针变量id，并将字 符数组keystr的首地址赋给id,即id指向keystr的第一个元素。
对程序分析如下：
□程序第14行定义了一个字符数组keystr,用于存放读入的关键字，也就是一个 字符串。C语言中的字符串本身就是以字符数组的形式存放的。
□程序第16?第22行是将各个字符串的首字符的地址分别赋给keyword的1号元 素?7号元素。经过赋值后，指针数组的元素就分别指向这7个字符串。赋值后 指针数组keyword的状态如图8.18所示。

