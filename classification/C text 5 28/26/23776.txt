char xdata px; 在xdata存器中定义了一^^指向字符类型(char)的指针。指针自 身在默认存贮区(决定于编译模式)，长度为2字节(值为0?OxFFFF)。
一、基于存贮器的指针
基于存C器的指针以存K:器类型为参量，它在编译时才被确定。因此，为指针选择存贮器 的方法可以省掉，以便这些指针的长度可为1个字节(idata ，data  , pdata  )或2个字节 (C〇de，xdata 〇。在编译时，这类操作一般被“行内”(iniine)编码，而无需进行库调用。 基于存贮器的指针定义举例：
char xdata  data pdx	除了明确定义指针位于8051内部存JC区(data)中外，其它与
上例相同，它与编译模式无关。
