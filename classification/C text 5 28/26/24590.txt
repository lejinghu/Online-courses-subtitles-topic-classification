case后面的常数表达式必须是整型、字符型、枚举类型。注意不可以是浮点类型。
各个case和default的出现次序可以是任意的。
在每一个case和default后面的语句最后，都有一个break语句。它表示当执行完当前 的case后面的语句之后，不再执行后面的case语句。
6.3.2	switch选择语句应用举例
下面我们通过一个实例来说明switch语句的使用。
case后面的常数表达式必须是整型、字符型、枚举类型。注意不可以是浮点类型。
各个case和default的出现次序可以是任意的。
在每一个case和default后面的语句最后，都有一个break语句。它表示当执行完当前 的case后面的语句之后，不再执行后面的case语句。
6.3.2	switch选择语句应用举例
下面我们通过一个实例来说明switch语句的使用。
