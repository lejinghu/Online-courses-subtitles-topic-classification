其中，类型名就是C语言中的数据类型。C语言中有许多数据类型，例如，int表示整 型，float表示浮点类型，char表示字符类型。变量列表是一个或者多个变量。如果是多个 变量，要用逗号分隔。
c语言规定，要使用变量，必须先定义变量。变量的定义格式如下：
类型名变量列表；
下面给出一些正确的变量定义形式：
1	int sum;	/★定义了 1个整型变量/
其中，类型名就是C语言中的数据类型。C语言中有许多数据类型，例如，int表示整 型，float表示浮点类型，char表示字符类型。变量列表是一个或者多个变量。如果是多个 变量，要用逗号分隔。
c语言规定，要使用变量，必须先定义变量。变量的定义格式如下：
类型名变量列表；
下面给出一些正确的变量定义形式：
1	int sum;	/★定义了 1个整型变量/
