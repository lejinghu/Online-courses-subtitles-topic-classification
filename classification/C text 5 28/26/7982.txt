到目前为止，我们使用的数据都是基本类型的数据，例如整型、实型和字符型。在 第4章我们介绍过，C语言中提供了对基本类型的支持，此外，C语言中还提供了对构 造类型数据的支持，这些类型包括：数据、结构体和共用体。实际上，构造类型数据是 由基本类型数据按照一定的规则组织起来的，因此也常被称为“导出类型”。
printf("%x\nM,a);
第16章数学计算支持
本章主要内容如下：
□浮点类型的特性；
到目前为止，我们使用的数据都是基本类型的数据，例如整型、实型和字符型。在 第4章我们介绍过，C语言中提供了对基本类型的支持，此外，C语言中还提供了对构 造类型数据的支持，这些类型包括：数据、结构体和共用体。实际上，构造类型数据是 由基本类型数据按照一定的规则组织起来的，因此也常被称为“导出类型”。
printf("%x\nM,a);
第16章数学计算支持
本章主要内容如下：
□浮点类型的特性；
