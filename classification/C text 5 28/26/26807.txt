s	实参必须为指向字符型数组的指针。输出数组中null之前的字符。当指定了精度时，不会输
g、G	根据指定了有效位数的精度，将double型的实参转换为f形式或e形式（指定G转换时为 E形式)。 精度为〇时，解释为1。 使用哪种形式取决于待转换的值。若转换结果中的指数小于-4或大于等于精度，则使用e形 式（或E形式）。无论使用哪种形式，都会去掉转换结果小数部分缀尾的〇。只有当小数点后 还有数字的情况下，才会输出小数点。
c	将int型的实参转换为unsigned char型之后，再输出转换后的字符。
出超出精度范围的字符。当精度未指定或精度大于数组长度时，数组必须包含null字符。
P	实参必须为指向void的指针。用编译器所定义的格式将该指针的值转换为可显示的字符序列。
