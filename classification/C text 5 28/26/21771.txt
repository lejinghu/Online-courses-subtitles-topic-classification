(1)	调用getchar读入一个字符并赋给字符变量c;
分析：以EOF (系统常量，值为-1)为结束标志的字符流称为一个正文，可以包含空白字符， 例如空格符、制表符和换行符。根据题目要求，输入函数应使用getchar (每次输入一个字符），输 出函数应使用putchar (每次输出一个字符）；复制过程是一个重复地调用getchar读和调用putchar 写的过程，因此程序的流程结构是一个循环语句；读入的字符是否为EOF则是循环控制条件。
算法步骤：
(2)	如果c不是EOF,则执行（3);否则结束执行；
(3)	输出c;
