(3)	通过库函数strcpy将一个字符串复制到一个字符型数组中。注意使用时要包含头 文件"string.h"。
scanf C%s-, x) ;	/输入一行字符串存放在x中?格式字符为s,注意x前面不加&符号/
gets和scanf的区别是gets输入的字符串可以包含空格或制表符。在使用scanf输入字 符串时，格式字符为这时数组名前面不加&符号，因为数组名已经是地址（详见后续 章节中有关“指针”的内容）。
char x[20]# y[20];
strcpy (x, "shanghai ■);	/	将字符串 _shanghai"复制到 x 中 /
(3)	通过库函数strcpy将一个字符串复制到一个字符型数组中。注意使用时要包含头 文件"string.h"。
scanf C%s-, x) ;	/输入一行字符串存放在x中?格式字符为s,注意x前面不加&符号/
gets和scanf的区别是gets输入的字符串可以包含空格或制表符。在使用scanf输入字 符串时，格式字符为这时数组名前面不加&符号，因为数组名已经是地址（详见后续 章节中有关“指针”的内容）。
char x[20]# y[20];
strcpy (x, "shanghai ■);	/	将字符串 _shanghai"复制到 x 中 /
