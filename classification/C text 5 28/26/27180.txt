图10.4利用字符型指针数组pa构造的字符串数组
Pa[l]
pa[2]
这里，指针数组pa的3个数组元素分别指向了长度不同的字符串（即无名一维字符数组），各 字符串所占存储单元之间并不一定连续，访问这些字符串全依赖于pa指针数组元素。一旦pa指
148
图10.4利用字符型指针数组pa构造的字符串数组
Pa[l]
pa[2]
这里，指针数组pa的3个数组元素分别指向了长度不同的字符串（即无名一维字符数组），各 字符串所占存储单元之间并不一定连续，访问这些字符串全依赖于pa指针数组元素。一旦pa指
148
图10.4利用字符型指针数组pa构造的字符串数组
Pa[l]
pa[2]
这里，指针数组pa的3个数组元素分别指向了长度不同的字符串（即无名一维字符数组），各 字符串所占存储单元之间并不一定连续，访问这些字符串全依赖于pa指针数组元素。一旦pa指
148
