注释无法在字符型常量或字符串型常量和其他注释中识别^ C语言实现不检査注释语句内容，
标准C语言中有两种编写注释的方式。传统上，注释语句以开头，以/结束c注释可以 包含任意多的字符，并总被作为空白符处理。
从C99开始，注释也可以用//开始，延伸到（但不包括）下一个行终结符。这个改变可能破 坏旧的C语言程序_但通常不会发生这种情况，读者可以想想什么情况下会发生这种问题。
除非识别（和越过）多字节字符与行终结符。 例下列程序包含4个有效C语言注释语句: // Pr〇gra?n to compute the squares of // the first 10 integers #include otdio
void Squares( / no arguments / )
注释无法在字符型常量或字符串型常量和其他注释中识别^ C语言实现不检査注释语句内容，
标准C语言中有两种编写注释的方式。传统上，注释语句以开头，以/结束c注释可以 包含任意多的字符，并总被作为空白符处理。
从C99开始，注释也可以用//开始，延伸到（但不包括）下一个行终结符。这个改变可能破 坏旧的C语言程序_但通常不会发生这种情况，读者可以想想什么情况下会发生这种问题。
除非识别（和越过）多字节字符与行终结符。 例下列程序包含4个有效C语言注释语句: // Pr〇gra?n to compute the squares of // the first 10 integers #include otdio
void Squares( / no arguments / )
