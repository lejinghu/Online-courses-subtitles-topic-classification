如果定义一个指针变量，该指针变量可以指向一个字符型数据，但是如果没有让该指针变 量指向一个确定的字符数据，它指向的位置不确定，则是一个危险的行为。例如，定义一个字 符指针变量，向该变量指向的内存单元输人数据可能会产生意料不到的错误。代码如下：
01	charstr[30];	/ 定义一个字符数组 str/
02	gets(str);	/正确，为字符数组str输入字符串7
01	charstr;	/定义一个字符指针变量str/
02	gets(str);	广可以运行，但是可能会产生意料不到的错误V
