□程序第26-31行定义了函数is_within(),该函数有两个参数，一个参数是字符指 针变量，且变量值在函数运行"过程中不改变，另一个参数是字符变量。函数体 中使用了 while语句将ch与str中的字符逐个进行比较，如果ch与当前比较的 字符不相等且当前字符不是>\〇’，那么就将str自增；否则，str当前指向的字符 就是要找的字符。最后使用return语句返回*str的值。注意：如果*str值为0, 表示一直搜索到字符串结尾也没有找到与ch匹配的字符，为其他整数值则表示 找到了与ch相同的字符。
口程序第17行调用is_within()函数，将input数组名和ch变量作为参数传递给该 函数。并将函数的返回结果赋值给found变量。
□程序第18~21行是根据is_within()函数的结果进行打印输出。如果found=0,则 表明在字符串input中未找到输入的字符；否则，表明在字符串input中找到了 输入的字符。
【示例12-11】编写一个函数string_in(),它接受两个指向字符的指针作为参数。如 果第二个指针指向的字符串被包含在第一个指针所指向的字符串中，函数就返回被包含 的字符串开始的地址。例如，string_in ("hats", "at")返回hats中a的地址，否则，函数 返回空指针。在一个使用循环语句为这个函数提供输入的完整程序中进行测试。
01	/* 文件名：12-11.c*/
□程序第26-31行定义了函数is_within(),该函数有两个参数，一个参数是字符指 针变量，且变量值在函数运行"过程中不改变，另一个参数是字符变量。函数体 中使用了 while语句将ch与str中的字符逐个进行比较，如果ch与当前比较的 字符不相等且当前字符不是>\〇’，那么就将str自增；否则，str当前指向的字符 就是要找的字符。最后使用return语句返回*str的值。注意：如果*str值为0, 表示一直搜索到字符串结尾也没有找到与ch匹配的字符，为其他整数值则表示 找到了与ch相同的字符。
口程序第17行调用is_within()函数，将input数组名和ch变量作为参数传递给该 函数。并将函数的返回结果赋值给found变量。
□程序第18~21行是根据is_within()函数的结果进行打印输出。如果found=0,则 表明在字符串input中未找到输入的字符；否则，表明在字符串input中找到了 输入的字符。
【示例12-11】编写一个函数string_in(),它接受两个指向字符的指针作为参数。如 果第二个指针指向的字符串被包含在第一个指针所指向的字符串中，函数就返回被包含 的字符串开始的地址。例如，string_in ("hats", "at")返回hats中a的地址，否则，函数 返回空指针。在一个使用循环语句为这个函数提供输入的完整程序中进行测试。
01	/* 文件名：12-11.c*/
□程序第26-31行定义了函数is_within(),该函数有两个参数，一个参数是字符指 针变量，且变量值在函数运行"过程中不改变，另一个参数是字符变量。函数体 中使用了 while语句将ch与str中的字符逐个进行比较，如果ch与当前比较的 字符不相等且当前字符不是>\〇’，那么就将str自增；否则，str当前指向的字符 就是要找的字符。最后使用return语句返回*str的值。注意：如果*str值为0, 表示一直搜索到字符串结尾也没有找到与ch匹配的字符，为其他整数值则表示 找到了与ch相同的字符。
口程序第17行调用is_within()函数，将input数组名和ch变量作为参数传递给该 函数。并将函数的返回结果赋值给found变量。
□程序第18~21行是根据is_within()函数的结果进行打印输出。如果found=0,则 表明在字符串input中未找到输入的字符；否则，表明在字符串input中找到了 输入的字符。
【示例12-11】编写一个函数string_in(),它接受两个指向字符的指针作为参数。如 果第二个指针指向的字符串被包含在第一个指针所指向的字符串中，函数就返回被包含 的字符串开始的地址。例如，string_in ("hats", "at")返回hats中a的地址，否则，函数 返回空指针。在一个使用循环语句为这个函数提供输入的完整程序中进行测试。
01	/* 文件名：12-11.c*/
