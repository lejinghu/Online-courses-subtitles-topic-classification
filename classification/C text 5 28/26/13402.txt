由于字符型指针可以指向字符串，因此字符指针数组也可以看作是字符串数组，但是 指向字符串的指针数组与存放字符串的二维字符数组有本质的区别。例如：
" 'L' V V 'e' " 'C 'h' ? 'n' 'a' '!' 0
图9-20利用字符指针将字符串倒序排列
char str [3] [10] = {’?Wuhan", "Beijing", "Shanghai" };
char *pstr[3];
由于字符型指针可以指向字符串，因此字符指针数组也可以看作是字符串数组，但是 指向字符串的指针数组与存放字符串的二维字符数组有本质的区别。例如：
" 'L' V V 'e' " 'C 'h' ? 'n' 'a' '!' 0
图9-20利用字符指针将字符串倒序排列
char str [3] [10] = {’?Wuhan", "Beijing", "Shanghai" };
char *pstr[3];
