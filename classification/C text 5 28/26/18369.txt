?第9行中，我们使用了 scanf_s()函数输入一个数字。第一个参数是一个字符串，称作 格式字符串(Format String)，字符串里面是%〇1，它叫做格式说明符(Format Specifier)，它限定 以什么格式读取这个数据。》紀代表整型，其他的还有%c表示字符类型等等。第二个参数是 存放这个输入数据的变量，这里我们存放到先前声明的变量number，在它的前面要加上符 号&，这个符号在这里的用法是取变量的地址，我们先不讲解地址的有关知识，您只需记住 要在这里一定要加上&，初学者们经常会遗忘它。
?第6行中，我们声明了一个变量number。
?第8行中，我们打印了一条提示语句。
?第10行中，我们使用了 printf()函数打印一行文字。第一个参数是一个格式字符串， 其中》/^的位置将会替换成第二个参数的变量，并以整型输出。“代表的是换行符，您可以 去掉\n试一试效果。

?第9行中，我们使用了 scanf_s()函数输入一个数字。第一个参数是一个字符串，称作 格式字符串(Format String)，字符串里面是%〇1，它叫做格式说明符(Format Specifier)，它限定 以什么格式读取这个数据。》紀代表整型，其他的还有%c表示字符类型等等。第二个参数是 存放这个输入数据的变量，这里我们存放到先前声明的变量number，在它的前面要加上符 号&，这个符号在这里的用法是取变量的地址，我们先不讲解地址的有关知识，您只需记住 要在这里一定要加上&，初学者们经常会遗忘它。
?第6行中，我们声明了一个变量number。
?第8行中，我们打印了一条提示语句。
?第10行中，我们使用了 printf()函数打印一行文字。第一个参数是一个格式字符串， 其中》/^的位置将会替换成第二个参数的变量，并以整型输出。“代表的是换行符，您可以 去掉\n试一试效果。

