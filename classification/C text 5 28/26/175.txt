如果字符串存放在一个字符型数组中，那么通过访问数组元素的方法就可以访问字符 串中的任何一个字符。
strlen("abc")的值是 3 strlen(m,) 的值是 0
12.2.5访问字符串中的字符
由于字符串以W结尾，因此通过如下的循环可以访问字符串S的每一个字符。
for (i = 0; s [i] ! =  \0 ? ; i + + }访问 s [i]	
如果字符串存放在一个字符型数组中，那么通过访问数组元素的方法就可以访问字符 串中的任何一个字符。
strlen("abc")的值是 3 strlen(m,) 的值是 0
12.2.5访问字符串中的字符
由于字符串以W结尾，因此通过如下的循环可以访问字符串S的每一个字符。
for (i = 0; s [i] ! =  \0 ? ; i + + }访问 s [i]	
