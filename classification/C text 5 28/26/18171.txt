因为本书中所使用的运行环境是VisualC++6.0,所以定义一个基本整型数据，它在内存 中占4个字节，也就是32位；如果定义一个字符型，则在内存中占1个字节，也就是8位。 不同的数据类型占用的字节数不同，因此占用的二进制位数也不同。
指点迷津：
在C语言中，内存单位的换算是以1024为标准，而不是1000。这就是为什么我们在购买 U盘、移动硬盘或者内存卡时，本来应该是2GB、40GB或512MB时，实际内存都不够，因为 商家一般是用1000为换算的，所以才会出现这种情况。
13.2位运箅操作符
_视频讲解：配套资源\mr\13\videoV?；运算操作符.exe
