>由于ch是字符型变量，因此，它也是一个整数，当用输出字符时，输出的是 该字符的ASCII码值65。
时，由于U是无符号数，因此u的值是65535 (BC下），4294967295 (VC下）。 但要注意，一定要用％u。
> L是有符号长整型，u是无符号整型。在BC下，L占4字节，u占2字节，当把u 赋值给L时，L的高2个字节为全0(因u无符号），故L的值为OxOOOOffif，即 65535。但在VC下，L和u都占4字节，当把u赋值给L时，L的值为Oxfffiffif， 即一 1。
为什么没有输出“I love C language!”呢？因为第24行的printf函数的第一个参数 中有个回车符V。当显示到\r时，光标又回到了行首，后面的“You”继续显示， 覆盖了 “I”以及后面的两个空格。
【思考题3-6】
