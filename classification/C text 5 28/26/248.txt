由于字符串存放在字符型数组中，因此定义和调用字符串作为参数的函数时，其格式 与数组作为参数是一样的，而且形参数组和实参数组也是同一数组。
}
14.2.3字符串作为函数的参数
字符串作为参数的不同点是：由于字符串有结束字符w，因而不需要把数组长度作为 参数。
如编写一个函数将一个字符串中的小写字母变为大写字母：
由于字符串存放在字符型数组中，因此定义和调用字符串作为参数的函数时，其格式 与数组作为参数是一样的，而且形参数组和实参数组也是同一数组。
}
14.2.3字符串作为函数的参数
字符串作为参数的不同点是：由于字符串有结束字符w，因而不需要把数组长度作为 参数。
如编写一个函数将一个字符串中的小写字母变为大写字母：
