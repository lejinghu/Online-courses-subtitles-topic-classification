(1)^可以是字符常置、字符变量或整型变量，当^是字符型数据时，输出的是字符本身；
功能：通过标准输出设备(显示器)输出一个字符。
说明：
当^是整型数据时，输出的是整型数据的八8(^1码对应的字符。
(2)<7也可以是转义字符，以控制一个动作。如： putchar( ^A^);	/*输出字符八*，
(1)^可以是字符常置、字符变量或整型变量，当^是字符型数据时，输出的是字符本身；
功能：通过标准输出设备(显示器)输出一个字符。
说明：
当^是整型数据时，输出的是整型数据的八8(^1码对应的字符。
(2)<7也可以是转义字符，以控制一个动作。如： putchar( ^A^);	/*输出字符八*，
