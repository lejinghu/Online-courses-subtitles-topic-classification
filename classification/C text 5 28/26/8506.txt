对括号类型名采用sizeof运算符得到指定类型的对象长度，即该对象类型占用的内存量 (存储单元数），包括内部或尾部的填充。根据定义，Bizeof运算符作用于任何字符类型时得到1。
■	unary，expression
表达式有两种形式：siMOf运算符加带括号的类型名或e〇f运算符加操作数表达 式。运箅结果是整数值常量，不能是左值。在标准C语言中，aizeof的结果是头文件Btddef.h中 定义的无符号整数类型■iztt。传统C语言实现通常用int或:long作为结果类型。按照C语言优先-级，sizeo￡(lcmg)，2解释为（sizeof (long))-2而不是Bisceof < (long) <-2) >。	[220
类型名可以是不完整数组类型（没有显式长度）、函数类型或void类型。
对表达式进行sizeof运箅得到的结果与对这个表迖式的类型名进行aieof运算得到的结 果相同。siaeof运算符使表达式确定类型时不必进行任何普通转换，这样就可以用8izeof取 得数组总长度，而不必将数组名转换成指针。但是，如果表达式中包含进行普通转换的运箅符，
