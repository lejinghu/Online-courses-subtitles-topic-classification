2.	已知head指向一个带有头结点的单向链表，链表中每个结点包含字符型数据域 (data)和指针域（next)。请编写函数实现在值为a的结点前插入值为key的结点，若 没有值为a的结点，则插在链表最后。
三、编程题
1.	建立一个链表，每个结点包括：学号、姓名、性别和年龄，输入一个学号，如果 链表中的结点包括该学号，则输出该结点内容后，将其结点删去。
第14章文件处理
在前面的各章中我们已经多次使用了文件，如源文件、目标文件、可执行文件和头 文件（库文件）等。文件在当今计算机系统中的作用是非常重要的，文件可以用来存放 程序、文档、数据、表格、图片和很多其他各类信息。作为一名编程人员，必须要掌握 创建文件、写入和读取文件的操作。
