字符变量在内存中占一个字节，可以存放ASCII字符集中的任何字符。当把字符放人字符 变量中时，字符变量中的值就.该字符的ASCII代码值，所以字符变量可以作为整型变量来处 理，可以参与对整型变量所允许的任何运算。例如：
C语言中，字符变量用关键字char进行定义，在定义的同时可以赋初值。例如：
Char chl	ch2 =' A ', ch3 ；
char cl, c2, c3; int k = 5 ； cl =' 1 ' ；	cl + + ；
c2 =' A'；	c3 = (c2 +32 + k)%25；
