其中argc和argv是两个参数名，可由用户自己命名，但是它们的类型却是固定的。第一个参数 argc必须是整型；第二个参数argv是一个指向字符型的指针数组的指针，这个字符型指针数组 的每个指针都指向一个字符串^因此第二个参数argv还可直接-义成基类型为字符型的指针 数组：
main ( int argc, char * * argv )
I … I
char * argv[]
当我们对包含以上主函数的、名为myc的文件进行编译连接，生成名为myc . exe的可执行 文件后，即可在操作系统提示符下输入以下命令执行该程序：
其中argc和argv是两个参数名，可由用户自己命名，但是它们的类型却是固定的。第一个参数 argc必须是整型；第二个参数argv是一个指向字符型的指针数组的指针，这个字符型指针数组 的每个指针都指向一个字符串^因此第二个参数argv还可直接-义成基类型为字符型的指针 数组：
main ( int argc, char * * argv )
I … I
char * argv[]
当我们对包含以上主函数的、名为myc的文件进行编译连接，生成名为myc . exe的可执行 文件后，即可在操作系统提示符下输入以下命令执行该程序：
