使用时要添加头文件stdiah,其中的参数ch为要进行输出的字符，可以是字符型变量或 整型变量，也可以是常量。例如输出一个字符A的代码如下：
为实现字符数据的输出，C语句库函数中提供了一个putcharO函数，作用是向显示设备输 出一个字符。这里需要注意的是putchar()函数每次输出一个字符。该函数的定义为：
int putchar( int ch );
putchar('A');
使用putchar()函数也可以输出转义字符，例如： putchar(MOr);
