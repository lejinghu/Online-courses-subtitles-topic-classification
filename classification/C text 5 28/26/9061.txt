本例中，a，b被说明为字符变量并赋予字符值，C语言允许字符变量参与数值运算， 即用字符的ASCII码参与运算。由于大小写字母的ASCII码相差32,因此运算后把小写字 母换成大写字母。然后分别以整型和字符型输出。
}

3.5.5字符串常量
字符串常量是由一对双引号括起的字符序列。例如："CHINA"， “C program”， "$12.5"等都是合法的字符串常量。
本例中，a，b被说明为字符变量并赋予字符值，C语言允许字符变量参与数值运算， 即用字符的ASCII码参与运算。由于大小写字母的ASCII码相差32,因此运算后把小写字 母换成大写字母。然后分别以整型和字符型输出。
}

3.5.5字符串常量
字符串常量是由一对双引号括起的字符序列。例如："CHINA"， “C program”， "$12.5"等都是合法的字符串常量。
