将整数赋值给字符型变量时，因为字符变量占用一个字节，而整型数据为两个字节， 因此要将字符的ASCII码放到整型变量的低八位中，而高八位为0。
这样赋值是允许的。
总结一下，整型和字符型可以互相赋值。
将字符赋值给整型变量时，具体分两种情况：
(1)	若所用系统将字符处理为无符号整数（即字符为unsignedchar型）时，则将八 位字符数据放入到整型的低八位中，而高八位补0;
将整数赋值给字符型变量时，因为字符变量占用一个字节，而整型数据为两个字节， 因此要将字符的ASCII码放到整型变量的低八位中，而高八位为0。
这样赋值是允许的。
总结一下，整型和字符型可以互相赋值。
将字符赋值给整型变量时，具体分两种情况：
(1)	若所用系统将字符处理为无符号整数（即字符为unsignedchar型）时，则将八 位字符数据放入到整型的低八位中，而高八位补0;
