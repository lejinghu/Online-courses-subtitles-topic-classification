前面我们己经接触过了 char类型的变量及字符型数组，但是对于字符串并没有过多 的阐述。本章将重点介绍字符串及它的一些相关操作。
?H 第12章字符及字符串处理 ?W 第13章内存处理 ?W 第14章文件处理 第15章底层程序设计 ??1第16章数学计算支持 ?H 第17章错误处理 ?N 第18章国际化特性
第12章字符及字符串处理
首先介绍字符串常量和字符串变量，C语言没有为字符串变量定义专门的类型，而 是把它们存放在字符数组中。但字符串变量并不等同于字符数组，字符串变量要使用特 殊的空字符来标识字符串的结束。C语言中为字符串的读写提供了几个库函数，使用它 们就可以完成字符串的输入和输出。此外，为了对字符串进行各种操作，C语言中还提 供了多个处理字符串的库函数，它们可以完成字符串处理的大多数常用功能。
本章主要内容如下：
前面我们己经接触过了 char类型的变量及字符型数组，但是对于字符串并没有过多 的阐述。本章将重点介绍字符串及它的一些相关操作。
?H 第12章字符及字符串处理 ?W 第13章内存处理 ?W 第14章文件处理 第15章底层程序设计 ??1第16章数学计算支持 ?H 第17章错误处理 ?N 第18章国际化特性
第12章字符及字符串处理
首先介绍字符串常量和字符串变量，C语言没有为字符串变量定义专门的类型，而 是把它们存放在字符数组中。但字符串变量并不等同于字符数组，字符串变量要使用特 殊的空字符来标识字符串的结束。C语言中为字符串的读写提供了几个库函数，使用它 们就可以完成字符串的输入和输出。此外，为了对字符串进行各种操作，C语言中还提 供了多个处理字符串的库函数，它们可以完成字符串处理的大多数常用功能。
本章主要内容如下：
