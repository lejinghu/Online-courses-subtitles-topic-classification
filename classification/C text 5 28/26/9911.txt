首先定义pl和p2是指向字符型数据的指针变 量。然后使字符指针变量pl和P2分别指向字符数组 stringl和string2的首地址。
图6-12字符指针pl和 p2同步移动
}
在for循环中，首先判断*pl是否为'\0'。假如不 为'\〇'，则进行* p2= * pl，它的功能是将数组stringl 中字符串的第一个字符赋给数组string2中的第一个 数组元素。然后再利用pl + +和P2 + +使pl和p2都 加1而分别指向各自的下一个数组元素，这样就保证
pl和p2同步移动	重复上述操作，将stringl数组
