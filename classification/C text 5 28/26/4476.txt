如果\是一个字符型变量，那么乂+3的值就会被截去一段，以便容纳于字符类型的变量中。那
在下面的语句中，认为3和\被赋予相同的值的说法是不正确的：
a = x = y+ 3;
么汪所赋的值就是这个被截短后的值。在下面这个常见的错误中，这种截短正是问题的根源所在：
char ch;
如果\是一个字符型变量，那么乂+3的值就会被截去一段，以便容纳于字符类型的变量中。那
在下面的语句中，认为3和\被赋予相同的值的说法是不正确的：
a = x = y+ 3;
么汪所赋的值就是这个被截短后的值。在下面这个常见的错误中，这种截短正是问题的根源所在：
char ch;
