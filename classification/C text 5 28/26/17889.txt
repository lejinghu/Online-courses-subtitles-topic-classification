(2)	程序中，定义了两个字符变量，cCharPut用来在程序中接收将要输入的字符，而
39	}
(1)	要使用字符函数，先要引入头文件ctype.h。
cCharTemp的作用是接收输入完成后回车确定的回车字符。
(3)	定义函数SwitchShowO实现程序中要进行判断字符的功能，这样可以使程序更简洁。 在SwitchShowO函数体中，通过在if语句的判断条件中调用字符函数，根据调用字符函数的返 回值结果判断传递的字符参数cChar是哪一种情况，最后通过在不同情况中的提示信息来表示 判断的结果。
