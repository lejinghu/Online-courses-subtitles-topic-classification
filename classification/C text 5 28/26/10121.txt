其中ch是要写人的字符常量或字符变量。fp是文件指针变量。fputc(ch，fp)函数完成 将字符ch的值写入fp所指向的文件上，并将文件位置指示器后移一位。若成功写入， fputc()函数返回所写人字符的值，否则返回EOF,EOF的值为一1。
fputc函数的功能是向一个已将打开方式指定为写或读写的指定文件中写入一个字 符。该函数的一般调用形式为：
fputc(ch, fp);
【例8-1】从键盘上输人一串字符，以回车键结束，建立一个文本文件in. txt。
#include< stdio.h>
