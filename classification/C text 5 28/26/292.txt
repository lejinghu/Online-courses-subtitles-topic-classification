字符串是指以W结束的一串字符，我们可以用字符型数组来存放字符串，也可以用一 个指针变量指向字符串。例如： char x[30]="abed”， p；
说明：程序首先定义了两个指针变fi p和q分别指向两个字符串的首字符。p++使得p 指向strl的下一个字符，q++使得q指向str2的下一个字符。其他说明见程序中的注释。
4.关于字符串的若干提法
这时字符串”abcd_?有如下几种提法：
(1)	由于"abed"存放在数组x中，因此被称为“存放在x中的字符串”：
字符串是指以W结束的一串字符，我们可以用字符型数组来存放字符串，也可以用一 个指针变量指向字符串。例如： char x[30]="abed”， p；
说明：程序首先定义了两个指针变fi p和q分别指向两个字符串的首字符。p++使得p 指向strl的下一个字符，q++使得q指向str2的下一个字符。其他说明见程序中的注释。
4.关于字符串的若干提法
这时字符串”abcd_?有如下几种提法：
(1)	由于"abed"存放在数组x中，因此被称为“存放在x中的字符串”：
