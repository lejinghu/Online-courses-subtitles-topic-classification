char ca = ^A'; /*说明字符变量〇8，且为其賦初值’六'*/
(2)变量名表是以逗号相间隔的、用标识符代表的变量。如:
char c;	/*说明字符变量。*/
2.	字符变量的存储
字符变量_经说明，计算机系统便为其分配一个字节，用于存放该字符。计算机系统中 存储的不是这个字符本身，而是这个字符对应的八50?马。如：吐批(^ = ’八_;则计算机系统 为其分配一个字节，存储字符'A'的ASC^码，即二进制 0100 0001(八进制101、十进制f)5、 十六进制41)。
