同样地，字符类型的赋值方法也可以像前面的一维数组的赋值一样，省略掉常量表达 式，上面的字符数组的定义及赋值代码也可以写出如下形式：
图8.20字符数组a的状态
其中，a[5]中存放的是空格字符，a[U]和a[12]中存放的是空字符‘\0’。
char c[] =	\ 1 a\ 1 \ f s\ 11\ 1 u\ 1 d\ ' e1n\ '	};
如果省略了常量表达式，则系统会根据初值个数确定字符数组的长度。字符数组c的 状态如图8.21所示。
