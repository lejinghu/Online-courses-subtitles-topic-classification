字符串的本质其实就是以'\〇’（即数字〇)结尾的字符型数组。字符串在内存中的起始地 址（即第一个字符的地址）称为字符串的指针，可以定义一个字符指针变量指向一个字符串。
假设定义了一指针变量int *q;，现将例9-5中的第13行?第15行换成q=p[i]; p[i]= p[j];p[j]=q;是否可行？结果是否相同？试比较两种方法对变量和指针数组有何影响？
9.5指针与字符串
1.字符串的表不
在C语言中，既可以用字符数组表示字符串，也可用字符指针变量来表示字符串。例 如，用字符数组str表示字符串”1 Love China!"可以这样来定义：
字符串的本质其实就是以'\〇’（即数字〇)结尾的字符型数组。字符串在内存中的起始地 址（即第一个字符的地址）称为字符串的指针，可以定义一个字符指针变量指向一个字符串。
假设定义了一指针变量int *q;，现将例9-5中的第13行?第15行换成q=p[i]; p[i]= p[j];p[j]=q;是否可行？结果是否相同？试比较两种方法对变量和指针数组有何影响？
9.5指针与字符串
1.字符串的表不
在C语言中，既可以用字符数组表示字符串，也可用字符指针变量来表示字符串。例 如，用字符数组str表示字符串”1 Love China!"可以这样来定义：
