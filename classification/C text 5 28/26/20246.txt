同样地，字符类型的赋值方法也可以像前面的一维数组的赋值一样，省略掉常量表达式， 字符数组的定义及赋值代码也可以写成如下形式：
图8.20字符数组a的状态
其中，a[5]中存放的是空格字符，a[ll]和a[12]中存放的是空字符’\0'。
char c〇={T,''a', 'm',''a',''s', 't', 'u', 'd', 'e', 'n', 't'};
如果省略了常量表达式，那么系统会根据初值个数确定字符数组的长度。字符数组c的状 态示意图如图8.21所示。
