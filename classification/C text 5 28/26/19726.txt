3.4.4字符型变量的应用
输出的第一项因为要输出的宽度小于字符串的实际长度，因此按照实际长度输出。输出的 第二项宽度为6列，但是只输出字符串的前3个字符，左端补上空格，靠右端输出。输出的第 三项只输出字符串的前3个字符，没有指定宽度，按实际宽度输出。
实际上，这些由双引号括起来的字符是一种特殊的字符，被称为字符串。
下面通过几个简单的例子对字符型变量的输出、输人进行学习，以加深对字符型数据 的认识。
【例3-7】先定义字符型变量和整型变量，然后以字符形式和整数形式输出。
