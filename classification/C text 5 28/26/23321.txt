可以看出，字符的存储形式与整数相似，不同的是，它在内存中仅占一个字节。C语言的 字符型数据与整型数据之间可以通用。
0100 0001表示火或65。
0110 000丨表示V或97。
除了以上的字符常量外，一些不可显示的控制宇符（比如回车键、Tab键、退格键等）在 程序中无法用一个?般形式的字符表示，C语言允许用一种特殊形式的字符常量来表示这些字 符：一个反斜杆“V’开头的字符，称为转义字符。如'W表示换行，它是将普通字符“转义” 成特殊字符。常用的转义字符有：
V	换行符（将当前位置移到下一行开头）
