②	char为字符型，在内存中占用一个字节的字符型数据，其实质是称为ASCII码的8位 二进制数，可以与int型教据通用。
本 f a
①	C语言的基本数据类型有char、int、long、float、double等几种。
③	inu丨ong表示的是整型数，只是各自表示的值的范围不同，每个数占用的空间不同， 其中，int为两个字节，long为4个字节，
④	float、double是浮点类型。浮点类塑的主要特点是表示的数值范围很大，但有一定的精 度限制? float占用4个字节，精度为7位；double占用8个字节，精度为15位。在计算机中， 整数和实数是有限的，数据的取值范围受到其类型的限制。
