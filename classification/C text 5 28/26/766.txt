量为字符变量，如：4扯&。由若干个字符构成且以％'结尾的字符序列称为字符串，字符串
1.字符数组与字符串的关系
在<：语言中，以一对单引号括起来的一个字符称为字符常最，如：_八'。用也肛说明的变
常量是以一对双引号括起来的一串字符，且以^0’作为结束标志，如："511?161^。（：语言提供
了字符这种数据类型，但没有提供字符串变凿。在〇语言中，字符串的存取可以使用字符数
量为字符变量，如：4扯&。由若干个字符构成且以％'结尾的字符序列称为字符串，字符串
1.字符数组与字符串的关系
在<：语言中，以一对单引号括起来的一个字符称为字符常最，如：_八'。用也肛说明的变
常量是以一对双引号括起来的一串字符，且以^0’作为结束标志，如："511?161^。（：语言提供
了字符这种数据类型，但没有提供字符串变凿。在〇语言中，字符串的存取可以使用字符数
