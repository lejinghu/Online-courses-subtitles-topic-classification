别说明，这些函数的参数既可以是字符串常量，也可以是字符型数组名，还可以是一个指向字符
打印该字符(如前所述，字符在本质上也是整型)。
同时，在函数库里存在许多操纵字符串的函数。这里我将简单地介绍几个最有用的。除非特
的指针。
_311\^函数与8131^^函数类似，但它并没有限_需要复制的字符数量。它接受两个参数：第2
别说明，这些函数的参数既可以是字符串常量，也可以是字符型数组名，还可以是一个指向字符
打印该字符(如前所述，字符在本质上也是整型)。
同时，在函数库里存在许多操纵字符串的函数。这里我将简单地介绍几个最有用的。除非特
的指针。
_311\^函数与8131^^函数类似，但它并没有限_需要复制的字符数量。它接受两个参数：第2
