可以在同一行定义多个相同数据类型的变量，例如，如果需要定义两个字符变量，可 以这样做：
/*后面的代码已省略*/
该不例代码有3个变量：answe、quantity和price。它们分别储存不同类型的 数据：字符数据、整型数据和浮点型数据。如果程序未定义这些变量，将无法储存数据。
main()
//程序中的变量 char first_initial; char middle_initial; /*后面的代码已省略★ /
