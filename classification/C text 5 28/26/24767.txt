其中，char是类型标识符，表示数据元素的类型是字符型；a是数组名，5是常量，表 示该数组包含5个字符类型的元素。
同理，定义一个包含5个字符类型的一维数组，代码如下：
char a[5];
2.	数组的名字——数组名
在一维数组的定义中，数组名的命名规则与变量名的命名规则相同，只能由字母、数 字和下划线组成。例如，以下数组名定义都是合法的：
其中，char是类型标识符，表示数据元素的类型是字符型；a是数组名，5是常量，表 示该数组包含5个字符类型的元素。
同理，定义一个包含5个字符类型的一维数组，代码如下：
char a[5];
2.	数组的名字——数组名
在一维数组的定义中，数组名的命名规则与变量名的命名规则相同，只能由字母、数 字和下划线组成。例如，以下数组名定义都是合法的：
