在字符型常量和字符串型常量中，引号中的字符不可以是单引号、双引号和反斜杠（\)。如果要 表示这些特殊的字符，需要使用转义字符表示。例如，要表示双引号，表示为（\”)。
例如，字符串"Hello”包括5个字符，它在计算机中存 储时，需要占用6个存储单元（6个字节)。其存储形式如 图3_11所示。
在字符串常量与字符常量中，所有元素都是字符。为什么有了子串常量，还要有字符串常 量呢？因为在编程过程中，使用字符串常量有时候会更省事、更方便。
3.5.5常量与常数
常量是由const定义和define定义（将在第10章讲到）的不可以改变的量。而常数则是C 语言中任何类型的数据。将常数定义为常量，主要由const定义。由define定义的常量称为符号 常量。代码如下：
在字符型常量和字符串型常量中，引号中的字符不可以是单引号、双引号和反斜杠（\)。如果要 表示这些特殊的字符，需要使用转义字符表示。例如，要表示双引号，表示为（\”)。
例如，字符串"Hello”包括5个字符，它在计算机中存 储时，需要占用6个存储单元（6个字节)。其存储形式如 图3_11所示。
在字符串常量与字符常量中，所有元素都是字符。为什么有了子串常量，还要有字符串常 量呢？因为在编程过程中，使用字符串常量有时候会更省事、更方便。
3.5.5常量与常数
常量是由const定义和define定义（将在第10章讲到）的不可以改变的量。而常数则是C 语言中任何类型的数据。将常数定义为常量，主要由const定义。由define定义的常量称为符号 常量。代码如下：
