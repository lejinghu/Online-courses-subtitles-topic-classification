如果定义一个指针变量，该指针变量可以指向一个字符型数据，但是如果没有让该指 针变量指向一个确定的字符数据，它指向的位置不确定，则是一个危险的行为。例如，定 义一个字符指针变量，向该变量指向的内存单元输入数据可能会产生意料不到的错误。代 码如下：
1	char str[30];	/定义一个字符数组str /
2	gets (str);	/正确。为字符数组str输入字符串/
1	char str;	/★定义一个字符指针变量str ★/
2	gets (str);	/★可以运行，但是可能会产生意料不到的错误★/
