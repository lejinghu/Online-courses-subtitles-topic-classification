(1)	<：语言中没有逻辑型数据，但€语言将任何非0的数据看成逻辑值真(1),将0看成 逻辑值假(〇),因此整型、实型、字符型数据都可以作为逻辑型数据》
II	或	双目	a||b	如果〇和6都为0，结果为0;否则，结果为1	—
说明：
(2)	逻辑运算符用于对数据进行逻辑判断|判断的结果只有2个(1或0)，系统在给出逻 辑运算的结果时，用1代表真，用0代表假。
(3)	逻辑运算符常用在分支和循环中，用于构造条件。可根据判断的结果决定程序后面 的流程应该如何进行。
