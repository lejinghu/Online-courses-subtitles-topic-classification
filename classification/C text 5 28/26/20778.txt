void sort(char strQ,int n)	/参数为指向字符型的指针数组/
它的调用形式与其他数组的调用类似。被调用函
数的首部代码如下：
这种调用方式也是将实际参数的地址传递给被调用函数的形式参数，在被调用函数中改变 形参的值也就是该实际参数的值。
(3)在被调用函数sort中，要将字符串按照字母大小排列，需要比较字符串中对应的每个 字符的大小。可以使用函数strcmp比较字符串的大小，代码如下：
