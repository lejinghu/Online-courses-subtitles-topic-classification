在某些计算机系统中（如\^€〇4/05，还有?6他11〇1茉列处理器)，字符类型被当作是有 符号的。这意味着当把一个字符转换为更大尺寸的数据类型如丨抗时，执行的是有符号扩展, 如果这个字符属于标准的厶8〇1字符集(其值属于0-127>,那么这一点不会给我们带来麻烦, 但是一且我们使用了非标准的字符（其值属于128-255)，那么扩展后的结果可能不是我们 所需要的。比如在Mac汁算机上的字符|\377 s在转换成为int类型时，其值将是-lr因 为将该字符看作是8位整数时，它是一个负值（最高位为1\
辟扩展
当一个有符号的整型数如519此3 11^或者319〇6(1北〇1^11^被转换为一个更大尺寸 的整型数时.（:语言执行的是有符号扩展，也就是说，使用原来数的符号位填充左面多出 来的二进制位1这一点可以保证在对负数（如-5)进行扩展时，扩展后的结果仍然是--个 负数（-5)6如果对无符号的整型数进行扩展，那么€语言将只是简单的用0填充左面多出来 的位。
为了避免这个问题，我们可以使用仙34：!以比31类型，将字符声明为无符号的，这
样在扩展的时候€语言将总是使用〇而不是符号位来填充多出的位^最终扩展所得的结果将
