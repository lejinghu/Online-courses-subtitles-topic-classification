这个例子定义了两个字符型变量cl、c2, —个字符型变景可存放一个字符。将一个字符常 量存放到一个字符变量中，实际上并不是把该字符常量本身放到内存中，而是将该字符相应的 ASCII码存放到内存单元中。C语言中的字符型数据与整型数据之间可以通用。
例2-3
char cl, c2； cl =，a，； c2 = V;
例2-4
#include <stdio.h> main()
这个例子定义了两个字符型变量cl、c2, —个字符型变景可存放一个字符。将一个字符常 量存放到一个字符变量中，实际上并不是把该字符常量本身放到内存中，而是将该字符相应的 ASCII码存放到内存单元中。C语言中的字符型数据与整型数据之间可以通用。
例2-3
char cl, c2； cl =，a，； c2 = V;
例2-4
#include <stdio.h> main()
