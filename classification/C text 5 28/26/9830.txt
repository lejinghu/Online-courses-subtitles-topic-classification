(2)	定义但不初始化，则自动赋以n〇”（整型和实型）或’\〇’(字符型）。
存储特点：
(1)	静态局部变量属于静态存储。在程序执行过程中，即使所在函数调用结束也不 释放。换句话说，在程序执行期间，静态局部变量始终存在，但其他函数是不能引用它 们的。
(3)	静态局部变量是在编译时赋初值的，即只赋初值一次，在程序运行时已有初值。 以后每次调用它们所在的函数时，不再重新赋初值，只是保留上次调用结束时的值。
使用静态局部变量的场合：
