程序中，while循环扫描整个s串。在while循环体内，每循环一次j自增，用以索引下一个元 素。当通过比较搜索到与字符变量c值相同的字符时，k停止自增，这样下一次循环时，后面的字 符就会将与字符变量c值相同的字符覆盖，达到删除与字符变量c值相同的字符的目的。
s[k]= f\0f;
}
【例7.18】编写将字符串反转的reverse函数。
字符串反转也称为字符串倒排，它将一个字符串首尾颠倒过来。如：将” abcde"颠倒为"edcba”。
程序中，while循环扫描整个s串。在while循环体内，每循环一次j自增，用以索引下一个元 素。当通过比较搜索到与字符变量c值相同的字符时，k停止自增，这样下一次循环时，后面的字 符就会将与字符变量c值相同的字符覆盖，达到删除与字符变量c值相同的字符的目的。
s[k]= f\0f;
}
【例7.18】编写将字符串反转的reverse函数。
字符串反转也称为字符串倒排，它将一个字符串首尾颠倒过来。如：将” abcde"颠倒为"edcba”。
