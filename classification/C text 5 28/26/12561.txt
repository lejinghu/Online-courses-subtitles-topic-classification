本程序首先以只读的形式打开文件"I:\filel.txt”，然后通过while循环语句把文本文件中的字符逐 个赋给字符变量ch并通过putchar函数输出，当遇到文件结束标志EOF时，退出循环，最后关闭文 件指针fp。
V〇u# 11 find ther-eJs no need to cry
分析
二、f^uts函数与fgets函数
对文件输入输出除以字符形式操作外，还可以以字符串为单位进行处理（即行处理）。在c语言 中，提供了 fj)uts函数和fgets函数实现字符串的读写。
