C语言没有字符串变量。在C语言中，字符串可以存放在字符型数组中。
"0577-86295984”
字符串中包含的字符个数称为字符串的长度。长度为0的字符串称为空字符串。C语 言规定，字符串必须以w结尾。\〇’是一个特殊的字符，它的ASCII码值是〇。字符串常 量的末尾自动包含W,如"beijing”实际上由V，’e’, T，了, ’i’, 'n’/g\ W组成。
12.2.2用字符型数组存放字符串
用字符型数组存放字符串，有如下几种形式。
C语言没有字符串变量。在C语言中，字符串可以存放在字符型数组中。
"0577-86295984”
字符串中包含的字符个数称为字符串的长度。长度为0的字符串称为空字符串。C语 言规定，字符串必须以w结尾。\〇’是一个特殊的字符，它的ASCII码值是〇。字符串常 量的末尾自动包含W,如"beijing”实际上由V，’e’, T，了, ’i’, 'n’/g\ W组成。
12.2.2用字符型数组存放字符串
用字符型数组存放字符串，有如下几种形式。
