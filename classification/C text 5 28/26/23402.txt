由此可见，“case'C'”及其后的语句和switch结构中的最后一条语句均被执行。 switch后面括号中的表达式一般是整型表达式或字符型表达式。case标号是整型数（包括 字符型）或整型常量表达式。注意：不能用变量作为case标号，每一个case常量表达式的值必 须互不相同。在case标号后面的语句部分如果有多条语句，则可以不必加花括号，它会自动依 次执行本case标号后面的语句，直到break语句为止。
请输入成绩等级（A、B、C、D) :C/ 及格
不及格
下面的程序在switch结构中有多个case标号共用一组执行语句,注意这个程序的运行结果。 例3-7
#include <stdio.h> main()
