(6)	声明一个指向字符型常量的指针pee。
(4)	声明一个字符指针ps并初始化，使ps指向字符串“12345”。
(5)	声明一个常量整型指针pci并对其进行初始化。
8.2 —个整型变量占4字节，其中每个字节又分成高4位和低4位。试从该整型变量的高字节开始，依 次取出每个字节的高4位和低4位，并以其值的ASCII码形式进行显示。
8.3将第7章中数组形式的冒泡法排序程序修改为指针形式。输入10个数，用指针形式冒泡排序法对其 进行排序，并输出排序后的结果。
