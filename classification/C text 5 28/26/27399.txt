这里ch是待输出的某个字符，它可以是一个字符常量，也允许是一个字符变量；fp是文件指 针。putc ( ch，fp )的功能是将字符ch写到文件指针fp所指的文件中去。如果输出成功，putc 函数返回所输出的字符；如果输出失败，则返回一个EOF值。EOF是在stdio.h库函数文件中定 义的符号常量，其值等于-1。
putc函数的调用形式如下：
putc ( ch, fp )；
fputc函数的调用形式和功能与putc函数完全相同。
例16.1把从键盘输入的文本按原样输出到名为file_a. dat的文件中，用字符@作为键盘 畅人结束标志。
