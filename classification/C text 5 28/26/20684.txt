其中，str是数组名，用来存放字符型数据。str表示字符数组的首地址，实质上它也是一个 指针，只是str是一个常量。数组元素在内存中是连续存放的，它的存放形式如图11.24所示。 在C语言中，也可以定义一个指针变量，使字符指针指向字符串的字符。代码如下：
程序的运行结果如下所示。
Happy New Year!
01	#include<stdio.h>
02	void main()
其中，str是数组名，用来存放字符型数据。str表示字符数组的首地址，实质上它也是一个 指针，只是str是一个常量。数组元素在内存中是连续存放的，它的存放形式如图11.24所示。 在C语言中，也可以定义一个指针变量，使字符指针指向字符串的字符。代码如下：
程序的运行结果如下所示。
Happy New Year!
01	#include<stdio.h>
02	void main()
