字符数组是用来存放字符数据的数组。常见的字符数据包括字符常量（如V，’B’，’!’等）、转义字 符常量（如_\t’，W等）、字符串常量（如"diabloir, "Warcraftm"等)、字符变量和符号变量等。
The number 5 in 3th line 2th row.
第四节字符数组
一、	字符数组的定义
字符数组可以通过数组名与下标结合进行定义，其一般格式如下： char数组名[下标1][下标2]…[下标n];
字符数组是用来存放字符数据的数组。常见的字符数据包括字符常量（如V，’B’，’!’等）、转义字 符常量（如_\t’，W等）、字符串常量（如"diabloir, "Warcraftm"等)、字符变量和符号变量等。
The number 5 in 3th line 2th row.
第四节字符数组
一、	字符数组的定义
字符数组可以通过数组名与下标结合进行定义，其一般格式如下： char数组名[下标1][下标2]…[下标n];
