位逻辑运算符有4个，这4个位运算符可用于整型数据和字符型数据，如表15.1所 示。将这些运算符称为位运算符的原因是它们可以对每个单独的位进行操作，而不会影 响其他的位。注意不要将位逻辑运算符与前面介绍过的常规的逻辑运算符相混淆（包括 &&、||和!），常规的逻辑运算符是对整个数值进行操作的。
C语言提供了 6个位运算符，这些运算符可以用于在整数和字符上进行按位运算。 在下面的例子中，我们将使用二进制记数法写出数值，这样就可以清楚地看出对位发生 的操作。在一个实际的应用程序中，则可以使用十进制、八进制或十六进制形式的整数 变量或常量。除了使用二进制以外，在下面的例子中，对每个字节从左到右进行编号， 编号从7到0。
15.1.1位逻辑运算符
表15.1位逻辑运算符
运算符	含	义
位逻辑运算符有4个，这4个位运算符可用于整型数据和字符型数据，如表15.1所 示。将这些运算符称为位运算符的原因是它们可以对每个单独的位进行操作，而不会影 响其他的位。注意不要将位逻辑运算符与前面介绍过的常规的逻辑运算符相混淆（包括 &&、||和!），常规的逻辑运算符是对整个数值进行操作的。
C语言提供了 6个位运算符，这些运算符可以用于在整数和字符上进行按位运算。 在下面的例子中，我们将使用二进制记数法写出数值，这样就可以清楚地看出对位发生 的操作。在一个实际的应用程序中，则可以使用十进制、八进制或十六进制形式的整数 变量或常量。除了使用二进制以外，在下面的例子中，对每个字节从左到右进行编号， 编号从7到0。
15.1.1位逻辑运算符
表15.1位逻辑运算符
运算符	含	义
