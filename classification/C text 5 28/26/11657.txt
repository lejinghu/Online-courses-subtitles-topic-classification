假定字符变量c为字符?0 ?至'9 |屮的一个，而且你希望将这个值转换为 对应的整数0至9。因为几乎所有字符集的字符数位都是用连续整数值表示的， 所以可以很轻松地将c转换为对应的整数值，只需要将该数位减去字符常量 ’O'即可。因此，如果i被定义为一个整型变量，则语句
尽管向一个字符常量加1似乎不太实用，但上面的例子给出了一种非常重 要的方法，可用于将字符W至W转换为对应的数字值0至9。回想一下， 字符W不同于整数0,字符不同于整数丨，以此类推。事实上，字符 在ASCII中的数字值为48,也就是以下printf ()调用的显示内容：
printf	'0')；
i = c - ' 0';
的效果就是将C中存放的字符数位转换为对应的整数值。假定C中存放字符 ?5'，它在ASCII中的数值为53。|〇?的ASCII值为48,所以执行上述语句 会从53中减去48，最终结果是将整数值5赋给i。在一台采用非ASCII字符 集的计算机上，尽管'5i和W的内部表示可能有所不同，但多半也会得到相 同的结果。
