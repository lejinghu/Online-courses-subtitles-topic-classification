式的后半段将字符变量=的值（其值为122)与最后一个小写字母_^进行比较。
字母的值依次类推)&因为厶30^^码的值大于97的字符不全是小写字母（比如大小括号),
因此为了准确判断宇符变置(：是不是小写宇母，还需要对于上限进行判断，所以上面的表达
因为在上面的表达式中，0语言编译器实际上是把字符变量€的值与小写宇母_^、4|
的八501值进行比较，所以下面的表达式实际上也可以用来判断字符变置。中包含的是否是
