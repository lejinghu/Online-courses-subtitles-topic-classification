字符常董的默认类型是int，除非前贾L: L显式地表示此字符常置的类型为宽字符类型 wchar_t。如果一个字符常ft包含一个可以用单字节表示的字符。那么其值就是运行字 符集中该字符的宁符码。比方说，在ASCII中，常量4 ?的编码是十进制的97。字符常 量的值如果包含一个以上的字符，可能在不同的编译器中会有不同的结果。
后面“转义符”一节会完整地介绍字符常董中允许使用哪些转义符。
字符常量的类型
下面的程序代码片段测试是否读进来的字符是1和5之间（含）的数字：
#include <stdio.h> int c = 0;
