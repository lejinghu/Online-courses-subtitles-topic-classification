fjnrtc的作用是将字符ch写人到帀指向的文件中。其中，ch是要写人文件的字符，它可以是 一个字符变量，也可以是一^字符常量。电是指向文件的指针变量。函数电伽具有返回值，如果 输出成功返回要输出的字符，否则返回EOF, EOF是C语言中定义的符号常量，被定义为-1。
将字符写人到文件中，需要调用函数f^utc,它的调用一般形式如下：
fputc(ch.fp);
函数finite与函数putchar类似，它们的作用都是输出字符。区别在于putchar用来将字符 显示在屏幕上，而fputc将字符输出到文件中。下面我们通过一个具体的实例说明函数f^utc的 使用。
【例14-1】通过键盘输入一些字符，将这些字符存人指定的文件中。
