设128,'a',8.5f,8.5四个数据依次为短整型、字符型、浮点型和双精度型，将它们依次写入到一 个二进制文件中，则它们在文件中的存储形式（十六进制）和顺序如表10.2所示。
在二进制文件中，每个字节都是对应数据在内存中存放时的表现形式，不同数据有不同的存储 长度，不同的存储格式。因此，在读写二进制文件文件时一定要注意读写数据的格式，例如，读出 的整型数据一定要赋给整型变量，读出的字符M数据一定要赋给字符型变量，读出的浮点型数据一 定要赋给浮点型变量，等等。
【例10.2】二进制文件的存储示例。
表10.2二进制文件的存储形式和顺序
序	号	内	容	序	号	内	容	序	号	内	容	序	号	内	容
设128,'a',8.5f,8.5四个数据依次为短整型、字符型、浮点型和双精度型，将它们依次写入到一 个二进制文件中，则它们在文件中的存储形式（十六进制）和顺序如表10.2所示。
在二进制文件中，每个字节都是对应数据在内存中存放时的表现形式，不同数据有不同的存储 长度，不同的存储格式。因此，在读写二进制文件文件时一定要注意读写数据的格式，例如，读出 的整型数据一定要赋给整型变量，读出的字符M数据一定要赋给字符型变量，读出的浮点型数据一 定要赋给浮点型变量，等等。
【例10.2】二进制文件的存储示例。
表10.2二进制文件的存储形式和顺序
序	号	内	容	序	号	内	容	序	号	内	容	序	号	内	容
