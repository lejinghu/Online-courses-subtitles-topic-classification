上面这段程序是将大写字母’A’?T转换成小写字母1a'?'z'并输出。注意最后输出时 仍是以整数形式来输出的。实际上字符在计算机内部还是以整数的形式来存储的，因此 循环变量c在形式上虽然是字符型，但实际上该循环还是以整数来计数的。
for (cs'A* ; c<=1 Z' ; C++)	/*for 语句*/
printf("%d\n", c+32);
(3)	for语句中的表达式2常用来判断是否达到循环次数，例如：
for(i=0 ; i<5 ;i++)
