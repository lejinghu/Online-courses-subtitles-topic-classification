实例11.10中定义了字符型指针变量string,用字符串常 量“\n见之而不知，虽识不妄；\n知之而不行，随敦必困。 \n”为其赋初值，注意这里并不是把“\n见之而不知，虽识 不妄；\n知之而不行，随敦必困。\n”这些字符存放到string
08	}
程序运行结果如图11.23所示。
中，只是把这个字符串中第一个字符的地址赋给指针变量string。

实例11.10中定义了字符型指针变量string,用字符串常 量“\n见之而不知，虽识不妄；\n知之而不行，随敦必困。 \n”为其赋初值，注意这里并不是把“\n见之而不知，虽识 不妄；\n知之而不行，随敦必困。\n”这些字符存放到string
08	}
程序运行结果如图11.23所示。
中，只是把这个字符串中第一个字符的地址赋给指针变量string。

