另外，C99标准为了让C和C++兼容，增加了一个头文件stdbool.h,里面定义宏名bool S_Bool 的同义词，定义false与true分别为0和1。因此，只要在源文件中包含stdbooLh这个头文件，就可 以在C语言里像C++那样使用bool定义布尔类型变量，通过true和false对布尔变量进行赋值。
C语言中可以用任何整数类型表示布尔值，0表示假，所有非0表示真。布尔表达式为假时值为0, 为真时值为1。例如，i=(a<b),在a小于b时对整型变量i陚值1;在a不小于b时对变量i赋值0。
C99标准引入了真正的布尔类型_B〇〇l, _B〇〇l类型长度为1,只能保存数值0和1 (分别表示 “false”与“true”）。虽然也可以使用其他整数类型表示布尔值，但如果C语言实现支持C99标准， 那么使用Bool类型更清晰。
将任意非零值赋值给_B〇〇l类型，都会先转换为1，表示真；将零值赋值给_B〇〇l类型，结果为 0，表不假。
【例2.24】捕鱼和分鱼问题：A、B、C、D、E五人在某天夜里合伙捕鱼，到第二天凌晨时都 疲惫不堪，于是各自找地方睡觉。A第一个醒来，他将鱼分为5份，把多余的一条扔掉，拿走自己 的一份。B第二个醒来，也将鱼分为5份，把多余的一条扔掉，拿走自己的1份。C、D、E依次醒 来，也按同样的方法拿鱼。问他们合伙捕了多少条鱼？
另外，C99标准为了让C和C++兼容，增加了一个头文件stdbool.h,里面定义宏名bool S_Bool 的同义词，定义false与true分别为0和1。因此，只要在源文件中包含stdbooLh这个头文件，就可 以在C语言里像C++那样使用bool定义布尔类型变量，通过true和false对布尔变量进行赋值。
C语言中可以用任何整数类型表示布尔值，0表示假，所有非0表示真。布尔表达式为假时值为0, 为真时值为1。例如，i=(a<b),在a小于b时对整型变量i陚值1;在a不小于b时对变量i赋值0。
C99标准引入了真正的布尔类型_B〇〇l, _B〇〇l类型长度为1,只能保存数值0和1 (分别表示 “false”与“true”）。虽然也可以使用其他整数类型表示布尔值，但如果C语言实现支持C99标准， 那么使用Bool类型更清晰。
将任意非零值赋值给_B〇〇l类型，都会先转换为1，表示真；将零值赋值给_B〇〇l类型，结果为 0，表不假。
【例2.24】捕鱼和分鱼问题：A、B、C、D、E五人在某天夜里合伙捕鱼，到第二天凌晨时都 疲惫不堪，于是各自找地方睡觉。A第一个醒来，他将鱼分为5份，把多余的一条扔掉，拿走自己 的一份。B第二个醒来，也将鱼分为5份，把多余的一条扔掉，拿走自己的1份。C、D、E依次醒 来，也按同样的方法拿鱼。问他们合伙捕了多少条鱼？
