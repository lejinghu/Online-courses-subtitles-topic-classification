bool型变量在程序设计中十分重要，它还有个特殊的名字：标志（flag)。例如按如下方 式声明一个布尔型变量：
这样的为了防止随后部分出现计算错误的条件测试叫做监护条件（guardh
4.3.4标志
bool done；
变量done便成了一个标志，可以用它来记录程序中是否完成了一定的操作。可以像对其他任 何变量一样给标志陚值。例如，可以写：
bool型变量在程序设计中十分重要，它还有个特殊的名字：标志（flag)。例如按如下方 式声明一个布尔型变量：
这样的为了防止随后部分出现计算错误的条件测试叫做监护条件（guardh
4.3.4标志
bool done；
变量done便成了一个标志，可以用它来记录程序中是否完成了一定的操作。可以像对其他任 何变量一样给标志陚值。例如，可以写：
