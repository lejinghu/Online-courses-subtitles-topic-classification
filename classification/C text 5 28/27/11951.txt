可以在程序中包含头文件<stdb〇〇l.h>,以简化对布尔变量的处理。这 个文件中定义了宏bool、true和false，使我们能够编写类似下面的语句:
随后可以使用诸如creal和cimag之类的库例程分别提取cl的实部和虚部。
一种实现不一定必须支持_Complex和_111\39：11131￥类型，也可以根据需 要选择支持一种而不支持另一种。
4.3派生数据类型
派生数据类型是由一个或多个基本数据类型构成的数据类型。派生数据类 型包括数组、结构体、联合体和指针。如果一个函数返回某种指定类型的值， 可以将这个函数看作一种派生数据类型。以下各节对这些派生数据类型进行了 概述（函数除外）。函数单独在7.0节介绍。
