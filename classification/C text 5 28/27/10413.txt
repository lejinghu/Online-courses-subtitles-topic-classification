现在就非常清楚flag不是普通的整型变量，而是表示布尔条件。（当然，编译器始终把flag看 成是int型变量。）后面的章节将介绍设置布尔类型的更好的方法（类型定义（>7.6节）和枚举 (>16.5 节））。
声明布尔型变量时可以用BOOL代替int:
BOOL flag；
5.3	switch 语句
在日常编程中，常常需要把表达式和一系列值进行比较，从中找出当前匹配的值。在5.2节 己经看到，级联式if语句可以达到这个目的。例如，下面的级联式if语句根据数字的级别显示 出相应的英语单词：
现在就非常清楚flag不是普通的整型变量，而是表示布尔条件。（当然，编译器始终把flag看 成是int型变量。）后面的章节将介绍设置布尔类型的更好的方法（类型定义（>7.6节）和枚举 (>16.5 节））。
声明布尔型变量时可以用BOOL代替int:
BOOL flag；
5.3	switch 语句
在日常编程中，常常需要把表达式和一系列值进行比较，从中找出当前匹配的值。在5.2节 己经看到，级联式if语句可以达到这个目的。例如，下面的级联式if语句根据数字的级别显示 出相应的英语单词：
