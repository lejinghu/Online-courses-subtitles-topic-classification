任何非零数值在转换为bool类型时，都得到true。这表示，可以把算术表达式的结 果赋予bool变量，如果它是非零值，就存储true,否则就存储false。
这些表达式称为逻辑表达式或布尔表达式，因为每个表达式都会得到两个结果之一： true或false。关系运算符生成布尔结果，所以可以把结果存储在bool类型的变量中。 例如：
bool result = 5 < 4;	// result will be false
注意：
等于运算符是两个连续的等号(==)，使用一个等号会出错。这是很容易混淆的。
