常量FALSE的内部值为0, TRUE的内部值为U使用这些数值作为数组booleanNames的下 标值就可以生成对应于布尔型值的字符串。这种情况也同样适用于连续枚举类型。
因为布尔类型在genlib.b中被定义成：
t-.ypedef enum {FALSE, TRUE) bool ;
11-6多维数组
在C语言中，数组元素可以是任何类型的。特别是，数组的元素又是数组。这种数组的数 组就叫做多维数组（muhidimensiormWrray),多维数组中最常见的就是二维数组，它常用于 表示分割成行和列的矩形的数据结构。这种二维结构的类型也叫做矩阵（matrix)。三维或是 多维的数组在C语言中虽然是合法的，但是很少出现^
常量FALSE的内部值为0, TRUE的内部值为U使用这些数值作为数组booleanNames的下 标值就可以生成对应于布尔型值的字符串。这种情况也同样适用于连续枚举类型。
因为布尔类型在genlib.b中被定义成：
t-.ypedef enum {FALSE, TRUE) bool ;
11-6多维数组
在C语言中，数组元素可以是任何类型的。特别是，数组的元素又是数组。这种数组的数 组就叫做多维数组（muhidimensiormWrray),多维数组中最常见的就是二维数组，它常用于 表示分割成行和列的矩形的数据结构。这种二维结构的类型也叫做矩阵（matrix)。三维或是 多维的数组在C语言中虽然是合法的，但是很少出现^
