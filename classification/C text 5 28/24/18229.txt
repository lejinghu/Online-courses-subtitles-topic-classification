从上面的结构中会发现使用typedef定义了一个FILE为该结构体类型，在编写程序时可直 接使用上面定义的FILE类型来定义变量，注意在定义变量时不用将结构体内容全部给出，只 需写成如下形式：
11	short token;	/*做正确性检测*/
12	}FILE;	
FILE文件指针名；
在使用FILE定义文件指针时，FILE要大写，它实际是由系统定义的一个结构体，该_构 体中含有文件名、文件当前位置和文件状态等信息。定义文件指针其实很简单，例如：
从上面的结构中会发现使用typedef定义了一个FILE为该结构体类型，在编写程序时可直 接使用上面定义的FILE类型来定义变量，注意在定义变量时不用将结构体内容全部给出，只 需写成如下形式：
11	short token;	/*做正确性检测*/
12	}FILE;	
FILE文件指针名；
在使用FILE定义文件指针时，FILE要大写，它实际是由系统定义的一个结构体，该_构 体中含有文件名、文件当前位置和文件状态等信息。定义文件指针其实很简单，例如：
