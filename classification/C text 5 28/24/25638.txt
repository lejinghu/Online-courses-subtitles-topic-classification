(1)	typedef是用来为数据类型重新命名的，不是用来定义变量的。
12.4.2使用typedef时应注意的问题
通过typedef可以为数据类型重新起个容易记忆的名字，这会使程序更加容易读懂。但 是使用typedef时需要注意以下几个问题。
(2)	用typedef是为已经存在的类型重新起了个新的名字，并没有产生新的类型。这就 类似于一个人有多个名字，但是只有一个人存在。无论使用哪个类型名定义变量，效果都 是一样的。例如，假设有如下的定义：
typedef int COUNT [20] ;	/ COUNT 是一个新的数组名★/
