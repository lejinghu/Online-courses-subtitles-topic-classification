(2)	根据变量定义时所指定的类型，就可以在编译时为变量分配相应的存储单元。
在使用变量之前必须先进行定义，这样做的优点如下：
(1)	将所有变量的定义放在一起，可以使读程序的人更易于了解程序的内容。如果 在变量命名时为其指定有意义的名字则更容易达到此目的。如果变量名不是有意义的名 字，那么还可以使用注释来解释变量所代表的含义。通过这些方式使得程序更易于理解， 因此这也是良好的编程技巧的体现。
(3 )便于在编译时根据变量的类型检查其所参与的运算是否是合法的。
因此，在C语言中变量应该遵循先定义，后使用的原则，这被认为是一项好的编程 技术，我们在写程序的时候都应该遵守。
