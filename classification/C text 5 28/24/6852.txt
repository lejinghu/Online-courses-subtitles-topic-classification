定义变量后就需要在内存中为其分配存储空间，如果定义变量后同时初始化变量， 则在内存中为定义的变量分配存储空间后还需要在空间中写入初值。例如：
int birds, chicken=40；
上面三条语句都对变量进行了初始化，需要说明的是最后一行语句仅对变量chicken 进行了初始化。这种写法容易引起误解，以为birds同时也被赋予了初值40,因此不推 荐这种写法，最好不要在一条定义语句中同时存在初始化的变量和未初始化的变量。
int books； int cats=20；
使用上面这两条语句定义变量后所对应的内存空间的状态如图4.4所示。
