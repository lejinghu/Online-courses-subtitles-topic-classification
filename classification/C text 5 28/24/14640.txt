(1)	类型与变量是不同的。例如只能对变量进行賦值操作，而不能对一个类型进行操作。这就像 使用int型定义变量ilnt,可以为ilnt进行賦值，但是不能为int进行陚值。在编译时，对类型是不分配 空间的，只对变量分配空间。
/*定义结构体变最*/
以上就是有关定义结构变量的3种方法。有关结构体的类型说明如下：
(2)	其中结构体的成员也可以是结构体类型的变量，例如：
struct date	r时间结构V
