变量初始化的格式是在定义变量时在变量名后面跟上一个赋值运算符（=)和要赋 给变量的初值。例如：
上面两条语句首先定义了一个整型变量books,接着使用scanf()函数为books变量 赋值。有关scanf〇函数的用法将在4.2.5小节中详细介绍。
除了上面两种为变量赋值的方法以外，还可以在定义变量时就为其赋值，即在定义 变量时给它一个初始值，这称为变量的初始化。
int books=12j
int cats=20, dogs=30；
