此处STREC是一个具体的结构体类型名，它能够唯一地标识这种结构体类型。因此，可用它来 定义变量，如同使用int、Char—样，不可再写关键字struct。
| STREC；
STREC std，pers [ 3 ]，* pstd;
>>14.2.3给结构体变量、数组赋初值
和一般的变量、数组一样，结构体变量和数组也可以在定义的同时赋初值。
此处STREC是一个具体的结构体类型名，它能够唯一地标识这种结构体类型。因此，可用它来 定义变量，如同使用int、Char—样，不可再写关键字struct。
| STREC；
STREC std，pers [ 3 ]，* pstd;
>>14.2.3给结构体变量、数组赋初值
和一般的变量、数组一样，结构体变量和数组也可以在定义的同时赋初值。
