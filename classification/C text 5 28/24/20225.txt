大家看到数组的好处了吧。从上面可以看出，定义数组与定义变量的方法一样简单。使用一 个类型标识符说明数组的类型，类型标识符后面就是数组，类似于之前我们的变量。上面的数组 a[50]表示可以存储50个元素，如果没有数组，就需要使用50个变量才能存储这么多的元素。也 就是说定义一个数组a[0]相当于定义了 50个变量，这就省事多了。那就让我们赶?决学习数组吧。
10	}	
我们发现，原来使用数组是这样简单！使用了一个数组a[5]就可以代替了 5个变量，还等 什么，赶快使用数组吧。在这里，a[5]是一个数组，可以存储5名学生的成绩。其实，数组a[5] 包含5个变量：a[0]、a[l]、a[2]、a[3]和a[4]，这5个变量可以分别存放一个整型的数据。同样 道理，如果要计算50名学生的语文成绩呢？很简单，只需要使用一个数组就省去了定义50变 量的麻烦，按照以下方式定义数组即可： int a[50];
8.1.2初识数组	）
例如a[50]就是一个数组，a被称为数组名，这与变量名非常类似。“[]”与其中的整数值被
