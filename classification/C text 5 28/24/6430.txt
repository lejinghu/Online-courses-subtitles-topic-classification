警告可以在struct声明语句的同时定义变量，但是绝大多数 C语言的程序炅都不会这样做。如果你想在声明结构布局的同时为 结构定义变量，可以在struct语句分号结束之前插入一个或多 个变量名。
4炅定义就是普通的内置数据类型定义（如前面例子中的int age;)。但是，你定 义的是成员，而不是变量。从本质上看，这是为结构的特殊部分命名。

结构要学习的内容很多。下面举个例子加深你对结构的理解。
假设你正在编写一个跟踪计算机零售库存的程序，要记录计算机的制造商、型号、磁 盘空间（MB)、内存空间（MB)、数量、成本和零售价。
