注意：程序在利用指针间接引用内存单元时，将按照指针变量定义时所指向的数据 类型来解释引用的内存单元。
printf ("a=%d, *p=%d", a, *p);
*是间接引用运算符，是单目运算符，优先级与++，一一的优先级相同，具有右结合 性。上面的程序中，与a++等价的表达式是(*p)+ +，而不是*p+ +。因为*p+ +相当于 *(p++)，其中的+ +是作用于p的，而不是作用于*p。
【例9-1】不同类型的指针操作同?内存变量。
1 2 3	#include <stdio.h>	
