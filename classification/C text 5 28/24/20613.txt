从而避免在定义第2个指针变量时漏掉运算符。这种形式的指针变量定义代码如下:
◎定义指针变量还有另外一种写法，就是将紧挨类型说明符，它的一般形式如下： 类型说明符指针变量名
这种写法也是正确的。这种写法的好处在于，如果定义多个指针变量，可以只用一个“”，
01	intp1,p2,p3;	/p1、p2和p3都被定义为指向int型的指针变量/
02	floatf1,f2;	/pf1和p3都被定义为指向float型的指针变量/
