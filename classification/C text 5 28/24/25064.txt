如果在函数内部定义变量，即局部变量不赋初值，对于static变量(整型)，编译时自动 赋初值为0。对于auto型变量，如果不赋初值，它的值就是一个不确定的值。
static int z=60;	/定义 static 变量 z 并赋初值★/
这样就只初始化一次变量的值。
对于static变量，只为变量分配一次内存单元。对于auto变量，每调用一次就分配一次 内存单元，程序调用结束，内存单元释放。
3.静态变量的使用——需要保留上一次的结果时使用static
