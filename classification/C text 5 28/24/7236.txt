不例9-1的程序中第6?13行声明了 一个结构体类型struct book,注意struct是声明 结构体类型时必须使用的关键字，不能省略。经过声明之后，struct book就可以在本程 序中使用，编译器不会报错。它与系统中提供的标准类型，如int和char等具有同等的 地位，都可以用来定义变量，区别是int等标准类型是由C语言系统定义的，而结构体 类型则是用户根据具体问题的需要在程序中来指定的。
C语言宝典作者：张三出版社：清华大学出版社出版日期：2014-6-16页数：560价格： 87.99
Press any key to continue.
声明结构体类型的一般形式为：
struct结构体名 {成员列表}
不例9-1的程序中第6?13行声明了 一个结构体类型struct book,注意struct是声明 结构体类型时必须使用的关键字，不能省略。经过声明之后，struct book就可以在本程 序中使用，编译器不会报错。它与系统中提供的标准类型，如int和char等具有同等的 地位，都可以用来定义变量，区别是int等标准类型是由C语言系统定义的，而结构体 类型则是用户根据具体问题的需要在程序中来指定的。
C语言宝典作者：张三出版社：清华大学出版社出版日期：2014-6-16页数：560价格： 87.99
Press any key to continue.
声明结构体类型的一般形式为：
struct结构体名 {成员列表}
