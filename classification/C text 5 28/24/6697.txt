2)	用typedef只能定义各种类型名，不能用来定义变量。
在进行C程序设计的时候，适当合理的使用typedef进行类型定义可以使程序的可 移植性大大增强。不过，有几点必须注意：
1)	用typdef进行类型定义，并不能产生新的类型，也不能取代原有的类型名，它 只是为原有的类型名增加一个新的替换名而已。
3)	C语言允许用户用typdef嵌套定义新类型名，即：用tydef定义了一个新的类型 名后，还可以用typedef将已定义的新类型名定义成另一个新类型名。
4)	用typedef既可以定义简单数据类型，也可以定义复杂一些的构造数据类型，如 数组、指针、结构、联合等。
