(2)用typedef是为已经存在的类型重新起了个名字，并没有产生新的类型。这就类似于一 个人有多个名字，但是只有一个人存在。无论使用哪个类型名定义变量，效果都是一样的。例
通过typedef可以为数据类型重新起个容易记忆的名字，这会使程序更加容易读懂。但是使 用typedef需要注意以下几个问题。
(1 ) typedef是用来为数据类型重新命名的，不是用来定义变量的。
如，假设有如下的定义：
typedef int COUNT[20];	/COUN 丁是一个新的数组名 /
