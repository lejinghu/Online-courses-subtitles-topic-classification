在上面的代码中，第1行定义了一个变量number,我们在前面介绍过，定义变量时 会为变量分配相应的存储空间。第4行则是变量的声明，告诉编译器此时要使用之前己 经定义过的变量number,不需要分配存储空间。关键字extern的使用也表明了第4行是 变量的声明，而不是定义，它指示编译器去其他地方寻找变量。
{
extern int number
而如果写成如下形式：
extern int number; main ()
