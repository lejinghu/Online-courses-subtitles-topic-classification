随后你就可以使用BYTE来定义变量了 ：
我们来看看typedef是怎样工作的■假设要对1字节的数值使用术语BYTE，你只须像 定义一个char变量那样定义BYTE，然后在这个定义前面加上关键字typedef，如：
typedef unsgned char BYTE;
BYTE x,y[10], * z;
该定义的作用域取决于typedef语句所在的位置■如果定义是在一个函数内部，它的 作用域就是局部的，限定在那个函数里■如果定义是在函数外部,它将具有全局作用域.
