如果运行以上代码，会出现以下错误信息：Undefined symbol‘b’in function main。意思 是在main函数中没有定义变量b。这说明在内部定义的变量，在外部不能使用。
12	}	
在该程序中，因为在外层复合语句中定义了变量a和c,在内层复合语句中定义了变量 b,只是在内层有效。
5.1.5为什么要有程序
为什么要有程序呢？语句可以完成一个任务中的简单操作，例如变量的赋值操作、两 个数的加减运算、输出变量的值。但是要让计算机完成任务，必须要有程序。
