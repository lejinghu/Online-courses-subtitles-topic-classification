指向函数的指针变量在使用之前必须要先定义，而且也必须进行初始化，使它指向某 个函数。另外，指向函数的指针变量定义中的返回值类型和参数表内容应该与该函数的 返回值类型和参数表内容保持一致。同时注意，指向函数的指针变量只能指向函数的人 口地址，而不能指向函数中间的某一条语句。
此时，由于（）优先级高于*，则仏11_?首先要与后面的（）结合，表示fim_p是一个函数；然 后再与前面的*结合，表示函数的返回值（即函数值）是指向整型变量的指针。这与前面 要定义的含义是完全不同的。
6.8.2利用函数指针变量调用函数
指向调用函数的一般定义形式为：
(*指针变量名）（实参表>;
