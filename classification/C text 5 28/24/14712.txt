本实例还使用之前声明过的学生结构。为结构体定义变量初始化赋值，然后使用指针指向该结构 变量。最后通过指针引用变量中的成员进行显示。
(*pStruct).iNumber=12061212;
【实例11.4】通过指针使用点运算符引用结构体变童的成员。（实例位里：光盘\TM\sl\ll\4)
#include<stdio.h>
int main()
本实例还使用之前声明过的学生结构。为结构体定义变量初始化赋值，然后使用指针指向该结构 变量。最后通过指针引用变量中的成员进行显示。
(*pStruct).iNumber=12061212;
【实例11.4】通过指针使用点运算符引用结构体变童的成员。（实例位里：光盘\TM\sl\ll\4)
#include<stdio.h>
int main()
