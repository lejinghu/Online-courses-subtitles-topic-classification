floata,b，c，x1，x2，delta;	/定义变量，a、b、c代表系数，x1和x2代表两个根，delta代表△/
2a	2a
a、b、c的值。因此，在程序中，需要定义变量a、b、c、xl和x2。xl和x2分别用来存放两个 根。另外，为了方便，可以设置一个变量delta存放b2-4ac的值。因此，在程序的开始，需要定 义6个变量分别是a、b、c、xl、x2和delta。代码如下：
2.	输入a、b、c的值
接下来，就要输入a、b、c的值。因为从方程的根可以看出，只有知道了 a、b、c的值， 才能得到xl和x2的值。输入a、b、c的代码如下：
