将程序划分为若干函数时，要考虑如何在代码中使用变量。局部变量定义在函数中， 且只能用于该函数。全局变量可用于在它定义之后的所有函数，不推荐使用全局变量。局 部变量更安全，因为可以限制只有需要使用局部变量的函数才能访问它们。在下一章中， 你将学到如何在函数间共享局部变量。
木章小结
本章教你使用堆积木的方式编写C程序。冗长的main ()函数就像是一本未划分章节 的书。太长的程序不方便使用和维护，应该将其划分为若干单独的函数，而且每个函数只 处理一个任务。
本章要点归纳如下。
■	在左花括号之后定义局部变量。在函数开始之前定义全局变量。
