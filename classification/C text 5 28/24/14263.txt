(1)	在程序中，还是定义变量iNumber表示1到100间的数字，不过刚开始未对其进行賦值， iSum表示计算的结果。
printffthe result is:%d\n",iSum); return 0;
}
(2)	使用for语句执行循环操作，括号中第一个表达式为循环变量进行陚值。第二个表达式判断 条件。条件为真，执行语句块中内容；条件为假，不进行循环操作。
(3)	在循环语句块中，进行累加运算。然后执行for括号中的第3个表达式，对循环变量进行自 增操作。循环操作后，将保存有计算结果的变童iSum进行输出。
