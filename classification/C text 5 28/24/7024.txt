在定义变量时，首先要指定变量的类型，然后再给出变量的名字。例如，下面的语 句定义两个变量：
(3 )便于在编译时根据变量的类型检查其所参与的运算是否是合法的。
因此，在C语言中变量应该遵循先定义，后使用的原则，这被认为是一项好的编程 技术，我们在写程序的时候都应该遵守。
int height； float profit；
其中第一条语句定义了一个int型的变量height，第二条语句定义了一个float型的 变量profit。
