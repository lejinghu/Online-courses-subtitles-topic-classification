上面的指针变量定义都是正确的。不同类型的指针变量指向不同类型的变量。例如，
float fl, f2;	/★定义两个指向float的变量fl和f2 /
char c;	/★定义一个指向字符型的变量c V
?
上面定义的pi和p2是int型指针变量，它们只能指向int型的变量，而不能指向其他类型 的变量。也就是说，将int型变量的地址存放到pi和p2中是正确的，而将float型变量的地 址存放到pi和p2中是错误的。
