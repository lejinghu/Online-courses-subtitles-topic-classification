在C语言中，不允许重复定义变量。当已经定义了变量a、b、c后，不能再次在该程 序中定义变量a、b、C，否则会出现错误。但在上面的代码中，在内层的花括号中定义了变 量a和b,不会出错。这是因为当在内层花括号中定义了与外层相同的变量后，在内层范围 内，会将外层的变量屏蔽掉，实际上起作用的就是内层的变量。在该代码中，有两对花括 号，它们的结构说明如图5.1所示。
c=12
c=600
	01		int a=3,b=4,c;
外层左花括号?	了——^		
