无嵌套结构的结构体变量的初始化可以分为3种情况：定义结构体类型和变量时同时 初始化、定义变量时初始化、先定义结构体类型再定义变量最后为变量赋初值。
与前面学过的变量一样，结构体变量在使用之前也需要初始化。结构体变量的初始化 就是在变量定义时为变量设置一个初始值。结构体变量的初始化可以分为两种情况：无嵌 套的结构体变量的初始化与嵌套的结构体变量初始化。
1.无嵌套的结构体变量的初始化
(1)	定义结构体类型和变量时，同时为变量赋初值。
【例12-1】在定义变量时直接为结构体变量赋初值：
无嵌套结构的结构体变量的初始化可以分为3种情况：定义结构体类型和变量时同时 初始化、定义变量时初始化、先定义结构体类型再定义变量最后为变量赋初值。
与前面学过的变量一样，结构体变量在使用之前也需要初始化。结构体变量的初始化 就是在变量定义时为变量设置一个初始值。结构体变量的初始化可以分为两种情况：无嵌 套的结构体变量的初始化与嵌套的结构体变量初始化。
1.无嵌套的结构体变量的初始化
(1)	定义结构体类型和变量时，同时为变量赋初值。
【例12-1】在定义变量时直接为结构体变量赋初值：
