也可以不定义变量p，而直接使用list进行迭代，具体为：
}
函数searchO的参数有两个，一个是list指针，它指向要搜索链表的第一个结点，另 一个是整数n,它就是要在链表中寻找的数值。在函数体中首先定义了指针P，然后在 for循环中为p赋初值list,让p指向链表中第一个结点，接着使用p依次指向链表中每 个结点。for循环中包含一条if语句，每次p指向链表中一个结点时都使用if语句进行 判断：该结点的value成员的值是否等于n,如果相等，则返回指针p当前的值，如果查 找了链表中所有结点都没有找到数值H,那么就返回空指针NULL。
struct node *search(struct node ?list,int n)
{
