11.2.2内存地址
一个64KB的计算机（用现在的标准来衡量属于小型的机器）的内存容量 是64x1024,即65 536字节^同样，一个8MB的工作站有8x1048576,
即8 388 608字节的容量^
在存储系统中，毎个宇节都会奋一个数宇的地址（address)。一般 来说，第一个字节在计算机中的地址为0,第二个字节的地址为U依 此类推，直到机器中所含有的字节数为止例如，你可以用右图表示 64KB的计算机的内存。内存中的每一个宇节可以容纳…个宇符的信息。
例如，如果你声明一个字符变量ch,编译器会在当前的函数帧中为这 个变量保留一个字节的空间。假设这个字节的地址为1000&如果程序 执行语句：
