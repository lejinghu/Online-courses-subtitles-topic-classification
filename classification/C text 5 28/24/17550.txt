(1)	在程序代码中，定义变量C。在for语句中先对c进行赋值，之后判断c<10的条件是 否为真，然后根据判断的结果选择是否执行循环语句。
12	return 0;
13	}
(2)	srand〇和rand()函数包含在stdio.h和stdlib.h头文件中，srand()函数的功能是设定一 个随机发生数的种子，randO函数是根据设定的随机发生数种子产生特定的随机数。
(3)	循环语句中使用srand()函数设定c+2为种子，然后使用rand()函数产生特定的随机发 生数，使用printf()函数将产生的随机数输出。 指点迷津：
(1)	在程序代码中，定义变量C。在for语句中先对c进行赋值，之后判断c<10的条件是 否为真，然后根据判断的结果选择是否执行循环语句。
12	return 0;
13	}
(2)	srand〇和rand()函数包含在stdio.h和stdlib.h头文件中，srand()函数的功能是设定一 个随机发生数的种子，randO函数是根据设定的随机发生数种子产生特定的随机数。
(3)	循环语句中使用srand()函数设定c+2为种子，然后使用rand()函数产生特定的随机发 生数，使用printf()函数将产生的随机数输出。 指点迷津：
