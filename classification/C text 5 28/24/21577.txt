int average;	/*定义变量total，用于存放平均值*/
为了便于给各个变量增添注释，同一类型的变量可以分开写成多个声明语句，上面一个声明语 句可以等价地写成如下两个声明语句：
int total;	/*定义变量total，用于存放总和*/
变量在定义时可以同时赋一个初值，其方法是：在变量名后跟一个赋值号“=”与表达式，这
个表达式被作为初始化符。这种赋初值的方法称为变量的显式初始化。下面是一些合法的声明语句: float eps=1.0e-5;	/* eps 为 float 类型，初值为 1 ? Oe-5 */
