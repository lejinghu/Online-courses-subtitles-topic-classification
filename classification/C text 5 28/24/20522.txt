要将变量定义为外部变量，需要使用关键字extern。 定义外部变量的方式如图9.22所示。
float a=10,b=20; 定义外部变量
图9.22定义外部变量的方式
在图9.22中，程序的最后定义了外部变量a和b,因 为变量的定义在main函数之后，所以变量无法在main函 数中起作用。为了让程序不出现错误，可以使用extern将 a和b的作用域扩展到main函数中。这就需要在语句 “s=fun(a，b);”之前有如下的外部声明语句：
extern float a,b; /将a和b的作用域扩展至main函数中/
