1.	指向指针的指针的变量定义
图11.39指针变量p指向数组的第_个元素
通过指针变量p也可以很方便地访问指针数组str中的字符串和字符串中的每一个字 符。因为p指向的是一个指针变量，所以p就是一个指向指针的指针。
要定义一个指向指针的指针变量，只需要在定义指针变量前面多一个 <7>。例如，定义一 个指向指针的指针变量P,代码如下：
	<7>p 则表示 str[2],而 str[2]指向了“Data Structure”。则*p 则指向了“Data Structure”。如果 有以下代码：
