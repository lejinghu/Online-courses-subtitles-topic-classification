共用体变量定义完成后，就可以引用其中的成员数据进行使用。引用的一般形式为: 共用体变量.成员名；
这种定义变量的方式与结构体定义变量的方式相似，不过一定要注意，结构体变量的大小 是所包括所有数据成员大小的总和，其中每个成员分别占有自己的内存单元；但是共用体的大 小为所包含数据成员中最大内存长度大小，例如在上面定义的共用体变量variable的大小就与 float类型大小相等。
12.7.2共用体变量的引用
例如，引用前面定义的variable变量中的成员数据的方法为： variable, ilnt; variable.cChar; variable. fFloat;
脚下留神：
