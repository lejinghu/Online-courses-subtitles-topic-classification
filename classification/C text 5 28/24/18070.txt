定义的数组包含5个元素，输出时使用for语句进行循环输出 操作。其中定义变量i用于控制循环操作用，因为数组的下标是从 0开始，所以为变量i赋值为0。
运行程序，显示效果如图12.7所示。
本程序将学生所需要的信息声明为struct Student结构体类型， 同时定义结构体数组student,并为其初始化数据。需要注意的是， 所给出数据的类型要与结构体中的成员变量的类型相符合。
在for语句中，先显示每个学生的输出次序，其中因为i的初 值为0,所以要加上1。之后将数组中的元素所表示的数据输出， 这时变量i作为数组的下标，然后通过结构体成员的引用得到正确 的数据，最后将其输出。
6$名：字号：12862213
