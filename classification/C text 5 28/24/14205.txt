(1)	在程序中定义变量iDay用来保存后面进行输入的数值。而其他的变量表示一周中的每
return 0;
}
—天。
(2)	在运行时，假设输入数值为6,代表选择星期六。if语句判断表达式iDay>Friday,如果成立 则表示输入的星期为休息日，否则执行else表示工作日的部分。如果判断为真，则再利用if语句判断 iDay是否等于Saturday变量的值，如果等于则表示为星期六，那么执行后面的语句，输出信息表示星 期六和朋友去逛街。else语句表示的是星期日，进行输出表示陪家人在家。
