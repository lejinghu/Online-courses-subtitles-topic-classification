命令脚本（command script)可以包含多行，每一行都必须以一个制表符（tab)开始。注 释和空白行会被忽略，命令脚本在遇到下一个目标行或变量定义时就会结束。 而且，命令脚本的第一行可以被放在“依赖性结尾分号”的后面，因此：
命令行选项-n会让⑽2k输出可以产生特定目标的命令。当测试makefile时，此选项是 不可或缺的（本章最后列出全部的mde选项。）。输出的最后一行对应到例19-1的第一 个规则。它显示削k会把$(CC)展开为gcc字样，把${LDFLAGS)展开为-lm。自 动变量$@和$^展开为目标circle和前提列表circle.o circulararea.o。在最前 面的两行输出中，自动变置被展开为一个前提，也就是需要被编译的C源代码文件名 称。
命令脚本
m迪 3ake
目标列表：{前提列表1 ;[命令 [命令 (...])]
