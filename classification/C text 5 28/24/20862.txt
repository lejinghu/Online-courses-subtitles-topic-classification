花括号内的每一个变量定义称为结构体的成员。例如，一个结构体类型的定义如下：
其中，结构体名是结构体类型的标志，结构体名的命名方法与变量名相同。花括号内的成 员列表是指由各种数据类型构成的变量定义列表。成员表列的定义格式如下：
类型说明符变量名；
01	struct student 02	{		/*结构体名*/
03	int num;	/*学奋*/
花括号内的每一个变量定义称为结构体的成员。例如，一个结构体类型的定义如下：
其中，结构体名是结构体类型的标志，结构体名的命名方法与变量名相同。花括号内的成 员列表是指由各种数据类型构成的变量定义列表。成员表列的定义格式如下：
类型说明符变量名；
01	struct student 02	{		/*结构体名*/
03	int num;	/*学奋*/
