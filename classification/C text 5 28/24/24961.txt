/函数类型为void,没有返回值★/ /定义变量/
函数min定义为int型，而return语句中的z为float型，两者的类型不一致。在第15 行中，执行return语句时，需要先将z的值即7.9转换为int型，即7,然后返回给调用函数 main。因此，输出的值是7,而不是7.9。
如果被调用函数中没有return语句，也就是不需要返回函数值，此时，函数类型应为 void(空类型)，表示函数没有返回值。例如，上面的main函数就是没有返回值的函数。 下面的函数也没有返回值：
/输出a与b的和★/
1	void print(int a, int b)
