“makefile依赖性规则”所提供的前提。 最后，例19-2的makefile和例19?丨不一样的地方在f,它没有为链接选项定义变量。反 而，它正确地列出C的数学链接库，作为c/rc/e可执行文件的前提，使用相同的-lm当 作编译器的选项。从输出的结果，我们会了解，mak如何展开此表示法，成为完整的链 接库文件名。
$(COMPILE.C) $(OUTPUT OPTION) $<
请注意，链接步骤是两个规则的组合i	自动采用内置规则定义的命令，并使用
隐式规则链
不管是内置规则，或者来自makefile的规则，只要目标不具有明显的规则，并带有命令 脚本，则会试着使用隐式规则。可能有许多隐式规则匹配同一个目标。比方说，
