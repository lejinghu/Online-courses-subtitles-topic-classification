(1)如果要让指针p指向结构体变量，需要将指针变量定义为指向结构体变量的指针 类型，代码如下：
程序运行结果如下： 10600,宋明，76 ? 50,经济管理系 10600,宋明，76.50,经济管理系
在上面的代码中，需要注意以下几点。
struct student p;	/★定义指向结构体类型的指针变量p ★/
(2)	为字符串或字符数组变量赋值时，不能直接用赋值符号赋值，以下代码是错误的: stu.name ="宋明"；/★错误丨试图以直接赋值的方式为stu的成员name赋值/
(1)如果要让指针p指向结构体变量，需要将指针变量定义为指向结构体变量的指针 类型，代码如下：
程序运行结果如下： 10600,宋明，76 ? 50,经济管理系 10600,宋明，76.50,经济管理系
在上面的代码中，需要注意以下几点。
struct student p;	/★定义指向结构体类型的指针变量p ★/
(2)	为字符串或字符数组变量赋值时，不能直接用赋值符号赋值，以下代码是错误的: stu.name ="宋明"；/★错误丨试图以直接赋值的方式为stu的成员name赋值/
