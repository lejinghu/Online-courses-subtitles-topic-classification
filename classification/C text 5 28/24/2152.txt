^上述列举的内容中，如果只在对应的源文件中使用，一般不放在头文件中。文件包含、 i不函数定义和变量定义也尽量不要放在头文件中。	^
void hello world(void);
(3)常量定义，包括具名常量和const常量，例如: #define ARRAY一SIZE 50 const int MAX 256
11.1.2	C工程的组织结构
如果一个C程序的规模较小，只需把程序的功能实现写在一个main函数中，并只保存在 一个源文件中，就足以保证其逻辑性和可读性。当程序的功能增多，操作比较复杂的时候，程 序内容放在一个main函数中会显得十分拥挤，程序逻辑比较混乱，此时，需要将程序划分成 多个功能独立的模块，将各个模块封装在一个函数内。
^上述列举的内容中，如果只在对应的源文件中使用，一般不放在头文件中。文件包含、 i不函数定义和变量定义也尽量不要放在头文件中。	^
void hello world(void);
(3)常量定义，包括具名常量和const常量，例如: #define ARRAY一SIZE 50 const int MAX 256
11.1.2	C工程的组织结构
如果一个C程序的规模较小，只需把程序的功能实现写在一个main函数中，并只保存在 一个源文件中，就足以保证其逻辑性和可读性。当程序的功能增多，操作比较复杂的时候，程 序内容放在一个main函数中会显得十分拥挤，程序逻辑比较混乱，此时，需要将程序划分成 多个功能独立的模块，将各个模块封装在一个函数内。
