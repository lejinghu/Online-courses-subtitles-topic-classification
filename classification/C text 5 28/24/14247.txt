/*定义变量，表示菜单的选项*/ /*检验条件，循环显示菜单*/
在使用程序时，根据程序的功能会有许多的选项，为了使用户可以方便地观察到菜单的选项，要 将其菜单进行输出。在本实例中，利用while将菜单进行循环输出，这样可以使用户更为清楚地知道每 一项选择所对应的操作。
#include<stdio.h> int main()
int iSelect=1;
while(iSelect!=0)
/*定义变量，表示菜单的选项*/ /*检验条件，循环显示菜单*/
在使用程序时，根据程序的功能会有许多的选项，为了使用户可以方便地观察到菜单的选项，要 将其菜单进行输出。在本实例中，利用while将菜单进行循环输出，这样可以使用户更为清楚地知道每 一项选择所对应的操作。
#include<stdio.h> int main()
int iSelect=1;
while(iSelect!=0)
