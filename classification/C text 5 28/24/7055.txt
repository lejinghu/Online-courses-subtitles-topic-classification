外部变量只能初始化一次，而且只能在变量定义的时候进行初始化。因此，下面的 语句是错误的：
{
此时代码的第1行就使用了 extern关键字，这显然是一个声明，不会为变量number 分配存储空间，而且编译器就会认为number的真正定义是在程序中的其他地方，或者 是在另外一个C语言源文件中。因此，如果确实需要分配存储空间，那么在定义外部变 量的时候就不要使用关键字extern,它只用来声明一个已存在的外部变量。
extern int number=5 ;	/*错误*/
这条语句中使用关键字extern,那么就表示该语句是一个变量的声明，并不是定义。
