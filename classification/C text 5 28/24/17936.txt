整型变量i地址是&i,其值传递给指针变量pi，则pi指向i,同时，将pi的地址&pl传 递给p2,则p2指向pi。这里的P2就是指向指针变量的指针变量，即指针的指针。指向指针 的指针变量定义如下：
_视频讲解：配套资源\mr\ll\video^向指针的指针.exe
一个指针变量可以指向整型变量、实型变量、字符型变量，当然也可以指向指针类型变量。 当指针变量用于指向指针类型变量时，则称之为指向指针的指针变量，这种双重指针如图11.28 所示。
类型标识符指针变量名；
例如：
