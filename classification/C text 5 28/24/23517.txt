其中，struct是C语言的关键字，不能省略；student是用户标识符；struct student是一 个用户自定义的结构体类型，它可以放在主函数的前面，或放在使用它的其他函数之前，它和 系统己定义的标准类型（如int、char、float型等）一样用来作为定义变量类型。
这个有关学生的记录类型可以用下面的方式进行定义： struct student
{ char no [10]; char name [ 8 ]; char sex [ 2 ]; char birthday [ 10 ]; int score;
另外，“学号”、“课程号”、“电话号码”、“邮政编码”等数据看似整型数据，但它们一般 不参与数学运算，且位数一般不变，因此宜釆用字符串类型。
7.2	皮义包构(本爰蓍的方法
其中，struct是C语言的关键字，不能省略；student是用户标识符；struct student是一 个用户自定义的结构体类型，它可以放在主函数的前面，或放在使用它的其他函数之前，它和 系统己定义的标准类型（如int、char、float型等）一样用来作为定义变量类型。
这个有关学生的记录类型可以用下面的方式进行定义： struct student
{ char no [10]; char name [ 8 ]; char sex [ 2 ]; char birthday [ 10 ]; int score;
另外，“学号”、“课程号”、“电话号码”、“邮政编码”等数据看似整型数据，但它们一般 不参与数学运算，且位数一般不变，因此宜釆用字符串类型。
7.2	皮义包构(本爰蓍的方法
