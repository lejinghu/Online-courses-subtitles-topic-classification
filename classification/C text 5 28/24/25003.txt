.函数的声明应放在调用函数变量定义之前，或者紧跟在变量定义之后。例如，以下函 数的声明方式也是正确的：
int multi () ;	/★参数类^和参数名被省略★/
上面的函数声明也是正确的。现在的编译系统也可以使用这种函数声明方式。
1
2
.函数的声明应放在调用函数变量定义之前，或者紧跟在变量定义之后。例如，以下函 数的声明方式也是正确的：
int multi () ;	/★参数类^和参数名被省略★/
上面的函数声明也是正确的。现在的编译系统也可以使用这种函数声明方式。
1
2
