9.11将9.10题中的字段结构变量和一个整型变量定义成一个联合类型。通过键盘输入一个整数，对字 段结构成员进行初始化，再将高字节和次高字节，以及低字节和次低字节之间数据交换，然后从该整型变量的 高字节开始，依次取出每个字节的高4位和低4位并以其作为十六进制值，再以该值的ASCII码形式进行显示。
(6)*(s.b+2) (7)*(++s.pb)+2 (8) (++s.pb) [3] (9)ps->pb[2]++ (10>*(++ps->pb)++
9.10将一个占4字节的整型变量，按每4位一组定义为字段结构。试从该整型变量的高字节开始，依次 取出每个字节的高4位和低4位并以其作为十六进制值，再以该值的ASCII码形式进行显示。
9.12请计算出表达式的值，各题的表达式相互无关。
typedef union U{ long x; unsigned char ua[4];}UTYPE;
