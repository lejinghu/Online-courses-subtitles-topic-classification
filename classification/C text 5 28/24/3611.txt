通过在所有函数定义的夕卜面（外部)定义变量units,它成为一个夕啸变量■要使units 对文件中随后的全部函数都可用，只需像前面这样做即可.
You must have looked it up!
注意函数critic()是怎样读取units的第二个值的；当main()结束while循环时，也知 道了新值■因此，两个函数main()和critic()都用标识符units来访问同一个变量.在C 的术语中，称units具有文件作用域,夕陪離接以及静态存储时期.
来看一些细节.首先,units声明所在的位置使得它对后面的函数可用,而不需采取任 何其他操作■这样,ctitics()就可以使用变量units.
与乀类似地，也不需要做仸何事来允许mian()访问units■然而，main()确实有如下声 明：
通过在所有函数定义的夕卜面（外部)定义变量units,它成为一个夕啸变量■要使units 对文件中随后的全部函数都可用，只需像前面这样做即可.
You must have looked it up!
注意函数critic()是怎样读取units的第二个值的；当main()结束while循环时，也知 道了新值■因此，两个函数main()和critic()都用标识符units来访问同一个变量.在C 的术语中，称units具有文件作用域,夕陪離接以及静态存储时期.
来看一些细节.首先,units声明所在的位置使得它对后面的函数可用,而不需采取任 何其他操作■这样,ctitics()就可以使用变量units.
与乀类似地，也不需要做仸何事来允许mian()访问units■然而，main()确实有如下声 明：
