这样PI就将PI定义为浮点型常量，常量值是3.1415926,它代表圆周率。在程序中用到圆 周率时，可以直接使用PI代替了。类似这样的方法称为常量定义。将书写比较麻烦的数据用常 量符号代替，避免在输人时出现错误。如果将3.1415926定义为常量，这样这个常量就被禁止 修改。
下面再举一例说明使用常量的好处——避免输人出错。例如，在计算一个圆的面积时，可 以将圆周率定义为常量，代码如下：
const float Pl=3.1415926;

在程序设计过程中，将保持不变的数据定义为常量是一种良好的编程习惯。在程序运行过程中， 变量是可以改变的量，常量在程序运行过程中被禁止改变。
