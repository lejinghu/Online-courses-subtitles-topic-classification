6本来是整型数据，在前面添加double后，就成了双精度浮点型。x被转换为单精度浮点 型，x+y被转换为基本整型。需要注意的是(int)(x+y)与(int)X+y是不一样的。前者是将x+y转换 为整型；后者只是将x转换为整型，然后与y相加。相应地，（float)x/3是先将x转换为浮点型 后再计算除以3。
02	(float)x/3;	/在变量x之前添加说明类型说明符float/
03	(int)(x+y);	/在变量x之前添加类型说明符int/
在对表达式进行类型转换时，得到的是一个中间数据，原来变量中的值是不变的。例如， (float)X是将x转换为浮点类型。如果x是整型变量3,贝U进行强制类型转换后，变量x中的值 仍然是3保持不变。

