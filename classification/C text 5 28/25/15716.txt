? 该类型称为双精度浮点是因为它的精度是浮点型Hoat的两倍。C语言开发早期蒈遍使用float类印，现 在则多用double来表示所嵙的浮点数。
尽管最初设计计算机是为了解决数值计算问题（因此计算机有时仍称作“number crunchen”）， 现代计算机处理的大多是文本而非数字 但由于数值计算非常简单 ，所以本书前 几章的程序设计例子将重点介绍如何处理数值数据，实际上，熟练掌握文本数据的处理也很 重要。
文本数据的最革本的元素是字符但将字符组合起来，形成一个连续的单元时才会更有用。 在程序设计中，一连串字符称为字符串，它使得在屏幕上显示消息成为可能。从hello.c开 始，读者B经在程序示例中见到了-些字符串D认识到字符串也是数据，可以用操作和存储 数值数据的方法来处理字符串，这一点很m要。
若用作者的名字来运行该程序，将得到如下的运行示例r
What is your name? BrlcJ Hello, Eric,
