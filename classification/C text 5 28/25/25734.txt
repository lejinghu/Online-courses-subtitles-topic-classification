例如，将整型变量a和浮点型变量f输出到指定的文件中的代码如下/
函数fprintf的主要作用是将数据输出到指定的文件中。它的一般形式如下： fprintf (文件指针，格式说明符，输出列表）；
其中，参数格式说明符与输出列表与函数printf中的参数是一致的，只是比函数printf 多了一个参数文件指针。
fprintf(fpf "%d,%f", a, f);
以上代码是将a和f输出到fp指定的文件中。如果a和f的值分别是5和7.5,则龟指 定的文件会存放以下数据：
