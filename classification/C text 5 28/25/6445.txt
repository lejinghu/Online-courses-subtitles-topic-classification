如果要传递多个变量，用逗号隔开。所有的变量都要列出数据类型，即使它们的类型 相同。下面所列的函数接收3个变量：一个浮点型、一个字符数组和一个整型：
printf( /*Rest of printf()1 return;
图31.2传递的是i的值，不是变量i
aFun (float x, char name [15] , int age) /★接收3个实参★/
警告按值传递保护了变量。如果接收函数改变了按值传递的变 量，不会影响主调函数的变量。因此，按值传递非常安全，因为 接收函数只能使用传递函数的变量，无法更改它们。
如果要传递多个变量，用逗号隔开。所有的变量都要列出数据类型，即使它们的类型 相同。下面所列的函数接收3个变量：一个浮点型、一个字符数组和一个整型：
printf( /*Rest of printf()1 return;
图31.2传递的是i的值，不是变量i
aFun (float x, char name [15] , int age) /★接收3个实参★/
警告按值传递保护了变量。如果接收函数改变了按值传递的变 量，不会影响主调函数的变量。因此，按值传递非常安全，因为 接收函数只能使用传递函数的变量，无法更改它们。
