程序执行到语句“cout?function(al，bl)?eiull;”时，自动查找与之匹配的子函数。由于子函数 ftmction(al，bl)中的两个形参al，bl都是整型，因此调用的函数应该是intfunction(intx，inty);语句 “cout?ftinction(a2，b2)?endl;” 中的两个形参都是浮点型，它与子函数float function(float x,float y) 匹配。二者不同的是后者在返回函数值时，把浮点型数据自动转换成整型。函数返回值的类型对函数 的重载不产生影响。
24.5^12.4-1.97581
分析
第六节构造函数与析构函数
给对象数据成员赋初值时，可以使用运算符“::”，通过调用成员函数的方法来实现，也可以通过 定义构造函数的方式来实现。析构函数与构造函数相反，当对象脱离其作用域时，系统自动执行析构 函数。
