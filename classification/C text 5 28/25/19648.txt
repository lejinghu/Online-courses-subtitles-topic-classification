01	float f;	/定义一个浮点型变量f/
从输出结果可以看出，用。/^格式输出小数点后6位。实际上，％e不指定输出的字段宽 度和小数的位数。默认输出6位小数，指数部分占5位宽度。如果要控制小数点的位数和输 出的宽度可以使用格式说明符。/om.ne。其中，m表示输出数据的宽度，小数的位数是n。如果 数据的宽度小于m,则在数据的左端补上空格。例如e+003，e占1位，指数符号占用1位， 指数占3位。
又如，下面的代码：
02	f=12.4567;	/为浮点型变量f赋值V
03	printf(”％e,%12e,%11.2e,%-11.2e\n",f,f,f,f); /输出变量f的值/
