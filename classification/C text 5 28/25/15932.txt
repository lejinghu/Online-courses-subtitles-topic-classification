2)	数组是同廇的。数组中的每个数值必须有同样的类型。因此你可以定义一个整型数组 或是一个浮点型数组，但是一个数组中不能有两种数据类型。
虽然你已绖知道程序操作数据，但本书前面部分重点讨论执行搡作的控制结 构，而不是基本的数据&在第11章?第17章，你将会着到t和控制结构一样, 数据结构也是程序设计的中心，通过学习将数据组装成较大的结构，能极大地 提高程序设计的能力，并能写出更有用和更激动人心的程序。
1)	数组是有序的D你必须能把数组中的每个分量按顺序排列：这是第一个，这是第二个， 等等a
直观上，我们可以把数组看成一系列的方框，数组中的每个数值占一个方框，数组中的
每一个数值称为一个元素（element) ?例如，右 					
