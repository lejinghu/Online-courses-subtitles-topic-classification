数的缩写。若要在程序中存储浮点型数据，就必须声明d〇uble型变量，就像在add2.c中 必须声明irat型变量一样a
print, f (<rThe total is %d.\n<l, total),■
虽然说printf函数的第一次调用并不是绝对役用，但即使不看前面的说明，不知道不同类型 的语句具体是怎么工作的情况下，也很可能看出这个程序的作用a它将两个数相加并显示结果。 也许更为重要的是我们可以对addle程序做一些修改来完成一些不同的功能。例如，可
要编写一个使用浮点型数值的完整的程序，必须先读取并显示double型数值。与整型数 —样，浮点数的读取也是用simpio库中的函数完成的。要读取浮点数，需要调用GetReal 函数，它的操作和本章前面2.2.1节中描述的Getlriteger函数完全一样，只不过它返回的是 一个double型的数值。在屏幕上显示浮点数也需要用到函数printf,但这时不是用％d而是 用另一种格式代码。浮点数有多种格式代码，最简单的是用％9，它代表一般的浮点数格式。
图2-3中的编程示例说明了把add2 .c程序修改为将两个浮点数相加的程序是很容易的，唯 一的不间仅在干改变了变量的类型，将函数Getlnteger换成了函数GetReal,将printf行 中的％d换成了
