(7)	输出数据时的域宽可以改变。若变量m、n、i和f都已正确定义并赋值，则语句printf (”％ *d"，m, i);将按照m指定的域宽输出i的值，并不输出m的值。而语句Primf("% *. * f”，m, n，f);将按照⑺和^指定的域宽输出浮点型变量f的值，并不输出m、n的值。
(5)	primf函数有返回值，返回值是本次调用输出字符的个数，包括回车等控制符
(6)	尽量不要在输出语句中改变输出变量的值，因为可能会造成输出结果的不确定性。例 如：4114=8;扣1^(_’％(},%(1\11”，11，++1〇;输出结果不是8,9,而是9,9。这是因为调用函数 printf时，其参数是从右至左进行处理的，将先进行+ + k运算。
3.3,数据输入
scanf函数是c语言提供的标准输入函数，其作用是从终端键盘上读入数据。
