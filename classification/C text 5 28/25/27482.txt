7.17此题与题7.16相似。函数的返回值为浮点型，而且函数只有一个形参，为整型。函数的基本算法是 累加，只是除第一项外其余各项都用减法。每一项的分子都是1,分母部分为k* k,k的值逐项增1,由2变化到 m。因此，算法可以用一个循环来实现。当m的值为12时，函数值应是:0.435023。
7.	16 函数中主要程序段：for( k = l;k<=n;k++) 1 s=-s;t = s*l. 0/k; add = add + t; |。注意，k 的 初值为-1，add的初值为0。当传给形参n的值为10时，函数的返回值add应当是:0? 645635。

7.18本题已给出了函数需要求值的表达式，读者只需确定函数的类型和形参的类型，就可以写出函数： double fun( double x ) j return x*x-5*x+4； }
读者应当写出正确的调用语句。
