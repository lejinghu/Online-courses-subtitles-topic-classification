/*字符型变量*/ r短整型变置v r整型变釐*/ r单精度浮点型v
ilnt=(int)fFloat;
printf("the char is: %c\n",cChar); printf("the long is: %ld\n",iShort); printf("the int is: %d\n",ilnt); printf("the float is: %f\n",fFloat);
r强制转化賦值v
r输出字符变置值v r输出短整型变置值" r输出整型变量值*/ r输出单精度浮点型变置值v
/*字符型变量*/ r短整型变置v r整型变釐*/ r单精度浮点型v
ilnt=(int)fFloat;
printf("the char is: %c\n",cChar); printf("the long is: %ld\n",iShort); printf("the int is: %d\n",ilnt); printf("the float is: %f\n",fFloat);
r强制转化賦值v
r输出字符变置值v r输出短整型变置值" r输出整型变量值*/ r输出单精度浮点型变置值v
