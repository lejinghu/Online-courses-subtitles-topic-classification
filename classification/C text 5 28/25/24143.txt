例如，一个储户的存款是111111111.55元，如果将该数存放到单精度浮点型变量f中， 因为单精度浮点型变量的有效位是7位，则只能精确表示前7位。则变量f将存款可能表示 为111111100.00,也可能表示为111111199.99。因为不能精确地表示储户的存款，所以不 是储户受损，就是银行受损。因此，为了表示足够的有效位，需要使用双精度浮点型变量。 双精度浮点型变量的有效位是16位，占用8个字节，它的表示范围(绝对值)0及是1.7xHT3()8 ?1.7xl〇3()8。在C语言中，对浮点型数据做处理时，都是将浮点数作为双精度数进行处理。
1.	为什么要使用双精度浮点型变量
为什么要使用双精度浮点型变量呢？这是因为双精度浮点型变量可以存放更加精确的 数据，表示范围更大。
2.	定义双精度浮点型变量——使用double
双精度浮点型变量的定义格式如下： double变量列表；
