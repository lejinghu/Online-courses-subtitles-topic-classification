在调用函数Max时，需要对参数和函数的返回值进行类型检查，因为参数“xyz”不能转 换为浮点型，所以出现错误。而在宏定义中，参数没有类型，它仅仅是一种符号。因此不会出 现错误。在C语言中，尽量使用函数，而不要使用宏定义，函数可以自动进行类型检查，排查 出潜在的错误，使程序不至于出现最后不能运行的错误。
运行该程序，出现以下错误结果提示。
error C2115: 'function' : incompatible types
10.2文件包含命令
前面我们不止一次地使用文件包含命令#inclUde，文件包含命令就是将一个源文件的内容包 含到另一个源文件中。当一个C语言程序文件中使用了另外一个文件中的函数，就需要将函数 所在的文件包含进来。本节主要讲解文件包含命$#include。
