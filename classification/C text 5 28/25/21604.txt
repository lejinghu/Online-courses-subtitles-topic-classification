逻辑运算符的操作数可以是整型、浮点型、复数类型和指针类型。两个操作数各自独立进行整 数提升，不进行一般的算术转换。逻辑表达式的操作数可以是值为0或非0的一个表达式。在C语 言中，〇值认为是逻辑“假”，非0值认为是逻辑“真”。认为是“假”的表达式有：具有0值的 整型表达式、具有0.0值的浮点型表达式、空字符’\0'和空指针。类似地，具有非0值的表达式都认
2.6.5逻辑运算
逻辑运算表示操作对象的逻辑关系，有3个逻辑运算符：！（单目，逻辑非）、&& (双目，逻 辑与）和|| (双目，逻辑或）。
为是“真”。
表2.7逻辑运算的真值表
逻辑运算符的操作数可以是整型、浮点型、复数类型和指针类型。两个操作数各自独立进行整 数提升，不进行一般的算术转换。逻辑表达式的操作数可以是值为0或非0的一个表达式。在C语 言中，〇值认为是逻辑“假”，非0值认为是逻辑“真”。认为是“假”的表达式有：具有0值的 整型表达式、具有0.0值的浮点型表达式、空字符’\0'和空指针。类似地，具有非0值的表达式都认
2.6.5逻辑运算
逻辑运算表示操作对象的逻辑关系，有3个逻辑运算符：！（单目，逻辑非）、&& (双目，逻 辑与）和|| (双目，逻辑或）。
为是“真”。
表2.7逻辑运算的真值表
