其中，fl、f2、f3、averagel和average2是单精度浮点数变量名。定义了变量之后，这些变 量就可以存储浮点型数据了。
01	float f1,f2,f3;	/定义了 3个单精度浮点型变量V
02	float average!,average2;	/定义了2个单精度浮点型变量/
2.中精度浮点型变S的取值范围
单精度浮点型变量占用4个字节，取值范围是-3.4x10-38 ~ 3.4x10'有效位是7位。有效 位是指从左边第一个非零的数字开始的数字个数，不包括小数点。例如，1.23456的有效位是6 位，0.0002345的有效位是4位，2前面的零不是有效位。因为C语言中单精度浮点型变量的有 效位是7位，如果超过了有效位，则超过的数据会不准确。例如，将一个浮点数123456789123.34 存人到变量a中，代码如下：
