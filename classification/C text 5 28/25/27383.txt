在此程序段中，最近一次是给共用体变量sl中的整型成员变量sl.i赋值，在输出语句中输出项是 共用体变量Sl中的浮点型成员变量sl.x。这时系统并不报错，但输出的结果既不会是123.4,也 不是100.0。系统将按照用户选择的成员类型（double).来解释公用存储区中存放的数值（100)。
sl. x = 123.4 ； sl.i = 100；
printf ( " % f\n" , sl. x )；
2.	共用体变量的整体赋值
ANSI C标准允许在两个类型相同的共用体变量之间进行赋值操作。设有：sl.i =5,则 执行：
