2.单精度浮点型变量的取值范围
2	float averagel/average2;	/定义了 2个单精度浮点型变量/
其中，fl、f2、fi、averagel和average2是单精度浮点数变量名。定义了变量之后，这 些变量就可以存储浮点型数据了。
单精度浮点型变量占用4个字节，取值范围(绝对值)是0及3.4XHT38?3.4xl038,有效 位是7位。有效位是指从左边第一个非零的数字开始的数字个数，不包括小数点。例如， 1.23456的有效位是6位，0.0002345的有效位是4位，2前面的零不是有效位。因为C语言 中单精度浮点型变量的有效位是7位，如果超过了有效位，则超过的数据会不准确。例如， 将一个浮点数123456789123.34存入到变量a中，代码如下：
1	float-f;	/★定义了 1个单精度浮点型变量f */
