在调用函数Max时，需要对参数和函数的返回值进行类型检查，因为参数“xyz”不能转 换为浮点型，所以出现错误。
运行该程序，出现以下错误提示结果：
error C2115: 1 function1 : incompatible types
而在宏定义中，参数没有类型，它仅仅是一种符号表示。因此不会出现错误。
在C语言中，应尽量使用函数，而不要使用宏定义，函数可以进行类型检查，会自动 进行类型检查，排查出潜在的错误，使程序不至于因出现错误的输入等而造成错误。
