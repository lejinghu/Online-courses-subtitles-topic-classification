4.	<float.h>:浮点型的特性
3.	<errno.li>:错误
<errno.h> (>24.2节）提供了eirrno (“error number”）。errno是一'个左值（lvalue)，可 ?以在调用特定库函数后进行检测，来判断调用过程中是否有错误发生。
〈float.h> (>23.1节）提供了用于描述浮点类型特性的宏，包括值的范围及精度。
5.	<limits.li>:整型的大小
