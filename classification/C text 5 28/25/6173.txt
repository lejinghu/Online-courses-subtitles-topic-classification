age并未变成浮点型变量，它只是为了这次计算而暂时地改变。程序中的其他地方并 未显式强制转换age，因此，它仍然是int变量。
不要频繁地对一个变量使用强制类型转换。只有在为了得到正确计算结果，不得不将 变量或表达式转换为另一种类型时，才使用强制类型转换。之前对age的强制类型转换 可以这样使用：
salaryBonus = salary * (float)age / 150.0;
#	警告如果将一个变量强制转换成另一种类型后，发现整个程序
使用的该变量都是转换后的类型，那么很可能在一开始就定义了 错误的数据类型。
