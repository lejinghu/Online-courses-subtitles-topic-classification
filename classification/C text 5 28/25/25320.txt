数组不仅可以存放整型、字符型、浮点型数据，还可以存放指针类型的数据。这样的 数组称为指针数组，数组元素为地址。指针变量不仅可以指向整型、字符型、浮点型数据， 也可以指向指针类型，这样的变量称为指向指针的指针变量即二级指针变量。
3	gets (str) ;	/★正确。因为str指向的位置已经确定/
11.5指针数组与指向指针的指针
11.5.1指针数组
指针数组就是用来存放指针类型的数组。通常使用指针数组存放字符串类型的数据。
数组不仅可以存放整型、字符型、浮点型数据，还可以存放指针类型的数据。这样的 数组称为指针数组，数组元素为地址。指针变量不仅可以指向整型、字符型、浮点型数据， 也可以指向指针类型，这样的变量称为指向指针的指针变量即二级指针变量。
3	gets (str) ;	/★正确。因为str指向的位置已经确定/
11.5指针数组与指向指针的指针
11.5.1指针数组
指针数组就是用来存放指针类型的数组。通常使用指针数组存放字符串类型的数据。
