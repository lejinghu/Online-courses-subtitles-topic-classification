scanfO并不是最容易使用的函数。scanf()存在的第一个问题是，用户输入的内 容必须与scanfO需要的内容精确匹配。其实，很难让用户做到这点。如果scanf()需 要一个浮点值，而用户输入了一个字符，你也无可奈何。此时，程序中的浮点型变量储存 的是不良数据，因为用户输入的字符不是浮点型的值。
提示用printf ()语句尽可能清楚地描述输入要求。在上例中， 如果只让用户输入一个喜爱的数字，用户可能会输入一个小数而 不是整数。天知道，也许有人就是喜欢3. 14159。
8.3	scanf()的问题
现在，先假设用户输入的都是程序所需的数据。在第19章中，将提供一些方案来解 决scanf ()导致的问题。（尽管现在C语言的程序员经常求助于其他的数据输入例程, 他们编写、下载或购买这些例程来克服C语言的数据输入困难。）
上一节中提到scanf 0要求在所有的变量前都加上&,这也有例外的情况。如果你打 算用％s将数据输入至一个数组中，只需让用户提供一个名称，再将其存入字符数组即可。 不需要使用&?
