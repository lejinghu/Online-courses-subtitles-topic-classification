(4)	表达式的实际数据类型要与格式转换符所表示的类型相符，printf函数不会进行不 同数据类型之间的自动转换。像整型数据不可能自动转换成浮点型数据，浮点型数据也不 可能自动转换成整型数据。下面例4-5中的程序的输出将不是所期望的。
输出的结果是3, 4, 3,而不是1，2，3。
(3)	格式转换符中，除了 X、E、G以外，其他均为小写。
【例4-5】错误的格式化输出。
1	#include <stdio.h>
