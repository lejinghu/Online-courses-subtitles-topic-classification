10.14设A、B都为3x3矩阵，元素为浮点型，从键盘对矩阵A、B的元素进行赋值，然后做AxB运算， 并将矩阵A、B的元素、结果元素都存盘保存。
10.12编写一个程序del，它用命令行指定的字符串替换命令行指定文件中的单词。例如，命令行： del file.txt they 将删除file.txt文件中的所有单词they。
10.13利用任意一种文本编辑器创建一个有50个整型数字字符串的文本文件matrix.dat,然后编程将50 个整型数字字符串依次读入，并转换成为整型数赋给5x5的矩阵A、B中的各元素，对矩阵A、B进行加法运 算，并将结果追加保存到文本文件matrix.dat中。
10.15设计一个通信录程序，包括姓名、性别、出生日期、家庭住址、工作单位、电话、手机号码、邮 件地址等字段。要求有录入、修改、删除、存盘、从文件读入等功能。
10.16编程将输入的“ABCD”写到文本文件中，然后再逐个字符读入，每进行一次字符的I/O操作都 把文件指针指向的_base、_ptr、_cnt等成员的值输出，以加深对FILE结构类型及其结构成员的理解。
