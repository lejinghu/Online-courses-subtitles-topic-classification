如果自变t是正无限大或负无限大，则isinf ()宏产生一个非零的值（表示tme),否则 isinf(}会产生0。自变量必须是一个实数的浮点数类型。“将浮点型态至少提升到双精 确度以进行数学计算”，这样的原则在这里并没有被采用：自变童的此项厲性是由实际的 类型和表示方式决定的。
测试某个浮点数值是否是无限大。
#include <math.h> int isin￡( float x )； int isinf( double x ); int isinf( long double x )；
范例
此函数如果遇到一个无限的加数，会采用快捷方式：
