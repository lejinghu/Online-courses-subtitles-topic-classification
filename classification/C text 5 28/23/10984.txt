答：在C语言被设计出来时还不是。在1978年的一篇论文中，Ken Thompson估计UNIX内核大约是10 000 行C代码（加上一小部分汇编代码）。UNIX的其他部分也有类似的大小。在另一篇1978年的论文中， Dennis Ritchie和他的同事将PDP-11的C编译器的大小设定为9660行。按现在的标准，这绝对只是小 型程序。
问与答
问：本节中提到C语言不是为'开发大型程序设计的。UMX不是大型程序吗？ （P.291)
问：C语言库中有什么抽象数据类型吗？
答：从技术上说，没有。但有一些很接近，包括FILE类型（定义在<31￡^〇上>中）。在对一个文件进行 操作之前，必须声明一个FILE *类型的变量：
答：在C语言被设计出来时还不是。在1978年的一篇论文中，Ken Thompson估计UNIX内核大约是10 000 行C代码（加上一小部分汇编代码）。UNIX的其他部分也有类似的大小。在另一篇1978年的论文中， Dennis Ritchie和他的同事将PDP-11的C编译器的大小设定为9660行。按现在的标准，这绝对只是小 型程序。
问与答
问：本节中提到C语言不是为'开发大型程序设计的。UMX不是大型程序吗？ （P.291)
问：C语言库中有什么抽象数据类型吗？
答：从技术上说，没有。但有一些很接近，包括FILE类型（定义在<31￡^〇上>中）。在对一个文件进行 操作之前，必须声明一个FILE *类型的变量：
