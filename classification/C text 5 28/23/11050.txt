答：当然。它可以是char *类型的任意表达式。这个特性使...printf类函数和...scanf类函数甚至比猜 想的缘由更加多样。请看卞面这个来自Kernighan和Ritchie所著的77ie CProgramm/ngiflngiwge—书的 经典案例。此案例显示出程序的命令行参数，以空格分隔： while (--argc > 0)
答：根据C标准，调用fflush函数的结果是把流定义为（1)为输出打开，或者（2)为更新打开并且流 的最后操作不是读。在其他全部情况下，调用fflush函数的结果是未定义的。当传递给fflush函 数空指针时，它会清除所有满足（1)或（2)的流。
问：在...printf类函数或...scanf类函数调用中，格式串可以是变量吗？
printf((argc >1) ?"%s" : "%s", *++argv);
这里的格式串是表达式(argc > 1) ? Is 4s",其结果是除了最后一个参数以外，其他 所有命令行参数都会使用Is"。
