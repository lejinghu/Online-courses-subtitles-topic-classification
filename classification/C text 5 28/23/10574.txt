答：虽然不要求，但是Kemighan和Ritchie写的Programming iangi/flge (第 1版）一直在return语 句中有圆括号。程序员（和后续书的作者）也采用K&R的这种习惯。.因为这种写法不是必须的，而 且对可读性没有任何帮助，所以本书不使用这些圆括号。（Kernighan和Ritchie显然也同意：在 ProgrflmmZngiflngi/flge (第2版）中，return语句就没有圆括号了。）
(3)把j乘以数组3中每个元素的大小；<4)把乘积的结果加上第二步计算出的地址?为了产生这些指 令，编译器必须知道a数组中每一行的大小，行的大小由列数决定。底线：程序员必须声明数组a 拥有的列的数童。
问：为什么一些程序员把return语句中的表达式用圆括号括起来？
问：如何测试main的返回值来判断程序是否正常终止？ （p.121)
答：这依赖于使用的操作系统。许多操作系统允许在“批处理文件”或“外壳文件”内测试main的返 回值，这类文件包含可以运行几个程序的命令。例如，
